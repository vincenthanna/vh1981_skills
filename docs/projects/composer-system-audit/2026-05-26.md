# Composer System Audit — 2026-05-26

> **무엇**: `prompts/prompt-composer-system/` 자체를 *진단*하는 1회성 audit 보고서. composer Phase 1-7 (G1-G7)을 거치지 않고, audit prompt 자체를 별도 layer(거버넌스·메타 진단 도구 — 4번째 축)에서 실행.
> **용도**: 시스템 변경 후 / 정기 점검. 발견 사항을 `self_upgrade.md` "1회 1대상" 단위 PR 제안으로 분리.
> **시리즈 위치**: 독립 실행 — composer Phase 밖, build 흐름에 호출 금지. (System Invariant ① 준수)
>
> Audit 입력: `.specs/composer-system-audit.composed.md` (Conv1 compose 산출)
> Audit 실행: Conv2 (본 conversation, 2026-05-26) — Phase 6 실 dispatch 금지, 모의 trace만.
> Audit 평가: Conv3 권고 (별도 LLM 또는 별도 세션 블라인드 채점)

---

## 0. Audit run 메타

- Auditor: claude-opus-4-7 (1M context), Claude Code 메인 세션 (`/Users/yeonhuigim/workspace/vh1981_skills`)
- Branch: `main`
- Working tree: clean (audit run에서 read-only 작업 + 본 보고서 Write만)
- `[VERIFIED:bash gh repo list DeepingSource @2026-05-26]` 모든 repo가 PRIVATE/INTERNAL — public repo 0건
- `[ASSUMPTION:self-eval audit]` 본 audit은 메인 세션 자기 진단이므로 self-eval bias 위험 — `[self-eval 경고]` 부착. 블라인드 독립 채점은 Step 5 권고에 명시.

---

## A.1 시스템 개관 — 인벤토리 표

### A.1.1 `prompts/prompt-composer-system/` (root)

| # | 파일 | size (B) | last_modified | layer | 호출 trigger | cost (rough) |
|---|---|---:|---|---|---|---|
| 1 | `CLAUDE.md` | 6,570 | 2026-05-26 | 진입 규약 (인프라 외) | 디렉토리 머리말 규약 SSOT | n/a (정적) |
| 2 | `optimized-prompt-composer.md` | 18,703 | 2026-05-26 | 조합 인프라 | composer Phase 1-7 orchestrator (메인 진입점) | ~2k |
| 3 | `task-spec-template.md` | 6,125 | 2026-05-20 | 조합 인프라 | Phase 1 Intake 입력 형식 | ~300 |
| 4 | `prompt-component-router.md` | 12,834 | 2026-05-26 | 조합 인프라 | Phase 2 Routing 로직 | ~500 |
| 5 | `context-injection-patterns.md` | 8,697 | 2026-05-20 | 조합 인프라 | Phase 3 Context Injection | ~500 |
| 6 | `prompt-evaluation-rubric.md` | 8,197 | 2026-05-20 | 조합 인프라 | Phase 5(pre) + Phase 7(post) | ~500 |
| 7 | `trigger-prompts.md` | 16,845 | 2026-05-21 | 인터페이스(entry) | A-1/A-2/A-3, C-1~C-7 시동 입력 모음 | ~1.5k (paste only) |
| 8 | `trigger.md` | 1,736 | 2026-05-21 | 인터페이스(entry) | A-2 trigger 단일 paste 본문 | ~300 (paste only) |
| 9 | `component-discovery-collect.md` | 8,228 | 2026-05-22 | 라이브러리 성장 (메타) | 외부 발굴 stage 1 (ledger append) | ~600 |
| 10 | `component-discovery-approve.md` | 9,991 | 2026-05-22 | 라이브러리 성장 (메타) | 사람 batch 승인 + component build | ~700 |
| 11 | `component-discovery-ledger.md` | 6,263 | 2026-05-26 | 라이브러리 성장 (공유 상태) | discovery SSOT (status table) | ~400 |

**합계**: 11 파일 (composed prompt §5.1 SSOT 목록은 10개로 적시 — `trigger.md`가 누락). →  [발견 A.2.1-F1, A.2.4-F2]

### A.1.2 `prompts/prompt-composer-system/components/` (7파일)

| # | 파일 | size (B) | last_modified | layer (CLAUDE.md 분류) | router §1.1 layer | trigger 분기 |
|---|---|---:|---|---|---|---|
| 1 | `agent-role-dictionary.md` | 33,722 | 2026-05-20 | 분석 컨텐츠 | (router §1 표 미표시) | role 다양화·critique 단계 |
| 2 | `multi-agent-analysis-template.md` | 61,483 | 2026-05-20 | 분석 컨텐츠 | direct-prompting bypass 충돌 가능 | topic ≥ 3, same criteria, decision |
| 3 | `code-review-rubric.md` | 12,565 | 2026-05-20 | domain content | (router §1 표 미표시 — §1.1 layer 누락) | spec.B=Review + diff |
| 4 | `experiment-design-template.md` | 16,226 | 2026-05-20 | domain content | (router §1.1 layer 누락) | spec.B=Analysis + ml/sys/hci-researcher + 가설 |
| 5 | `rfc-writing-template.md` | 16,801 | 2026-05-20 | domain content | (router §1.1 layer 누락) | spec.B=Generation + team/external + RFC |
| 6 | `autonomous-optimization-loop.md` | 16,553 | 2026-05-20 | domain content | (router §1.1 layer 누락) | 단일 스칼라 + REVERSIBLE |
| 7 | `speckit-spec-generation.md` | 10,944 | 2026-05-26 | domain content (planning/SDD) | **layer 필드 명시 (router §1.1과 동시 정의)** | Generation + spec/plan/tasks 키워드 |

**관찰**: 7 component 중 6개의 `## 0. Router 등록 metadata`에 **`layer` 필드 미정의**. speckit만 신규로 명시 — 나머지는 CLAUDE.md "컴포넌트 라이브러리" 절의 텍스트 분류에 암묵적 의존. router §1.1이 `layer` 필드를 SSOT로 선언했지만 실제 component 파일에서 echo되지 않음. → [발견 A.2.4-F1]

### A.1.3 repo root 보조 파일

| 파일 | 존재 | last_modified | 용도 |
|---|---|---|---|
| `README.md` | ✓ | (조회 안함) | plugin marketplace · prompts-pack · worklog 안내. **prompt-composer-system 절 없음** — [발견 A.4-F1] |
| `self_upgrade.md` | ✓ | (조회 안함) | 거버넌스 SSOT — Loop A(지식 갱신) + Loop B(프롬프트 개선) + 채점 정책 §B4 |
| `self_upgrade-changelog.md` | ✓ | (조회 안함) | A/B run 1줄 기록 |

---

## A.2 Checklist 4항목 평가

### A.2.1 built-prompt가 rough request를 손상시키는가?

**평가 절차**: `task-spec-template.md` A-E 필드 → composer §5 Phase 4 §1-§7 매핑이 *손실 없이* 이뤄지는가. Phase 4 발췌 가이드와 spec.E echo가 누락되는 분기가 있는가.

**평가 근거 인용**:
- composer §5 Phase 4 layout: `### 3.1 Purpose <spec.A — 그대로>`, `### 3.2 Output Specification: Type/Form/Length`, `### 3.3 Constraints`, `## 6. Output Gates` (spec.E echo). [`optimized-prompt-composer.md:170-211`]
- spec.E echo 강제: Gate G4 체크리스트에 "composed prompt가 §1-§7 모두 있음"만 있음 — *spec.E의 각 criterion이 §6에 1:1 echo되는지* 명시적 체크 없음. [`optimized-prompt-composer.md:247-252`]

**발견 사항 (3건)**:

- **F1. trigger.md 표준 머리말 위반**: `prompts/prompt-composer-system/CLAUDE.md:28`은 "trigger.md처럼 통째로 복사해 쓰는 prompt 파일도 머리말을 붙이되, '아래 구분선부터가 복사 본문'임을 명시한다"고 규약하지만, `prompts/prompt-composer-system/trigger.md:1-3`은 표준 3필드 머리말(무엇/용도/시리즈 위치) 0건이고 첫 줄부터 본문(`너는 지금부터 optimized-prompt-composer로 동작한다.`)이다. **영향**: rough request 손상은 아니지만 *자기 규약 위반* — 외부 사용자가 trigger.md를 보면 "어디까지가 머리말이고 어디부터가 복사 본문인지" 식별 불가. Severity: **MEDIUM**.
- **F2. trigger.md ↔ trigger-prompts.md A-2 DRY 위반**: 둘 다 같은 A-2 trigger 본문을 보유. `trigger-prompts.md:91-122`가 정본인데 `trigger.md`는 그 paste-ready 변형. trigger-prompts.md A-2가 업데이트되어도 trigger.md는 silent drift. **영향**: rough request 처리는 동일하나 *시스템 일관성* 손상 가능. Severity: **MEDIUM**.
- **F3. spec.E echo 명시 게이트 부재**: G4(`optimized-prompt-composer.md:247-252`)는 "composed prompt가 §1-§7 모두 있음"만 검사 — spec.E의 각 success criterion이 §6 Output Gates에 echo됐는지 *grep 단위로* 확인하는 규칙 없음. P1(spec coverage, `prompt-evaluation-rubric.md:24-28`)이 이 역할을 하지만 P1 검사 시점은 G5(Phase 5)다. **결과**: Phase 4에서 spec.E 항목 누락이 Phase 5에서야 발견되어 회귀 1회 추가 비용. Severity: **LOW**.

### A.2.2 DeepingSource repo 도메인에 필요한 component 커버리지

**평가 절차**: DeepingSource public repo 인벤토리 → 사용 언어 / AI 모델 / 인프라 추정 → 기존 7 component + 11 role lens(role-dict §5)에 대한 매핑 점검.

**Repo 인벤토리** `[VERIFIED:bash gh repo list DeepingSource --limit 30 --json ... @2026-05-26]`:
- **모든 30개가 PRIVATE 또는 INTERNAL** — public repo 0건. 따라서 *내용 조회 없이 metadata만* 사용 (이름·언어·description). 이는 audit composed prompt §3.3 "민감정보: DeepingSource public repo만"의 strict 해석으로는 *abort 후 사용자 보고* 신호. 본 audit은 metadata-only inference로 진행하되 결과 정확도 한계를 명시 — Step 5 보류 항목 참조.
- **언어 분포**: TypeScript 11, Python 9, Shell 2, JavaScript 1, C++ 1, MDX 1, Dart 1, null 2 (총 28).
- **도메인 추정** `[ASSUMPTION:metadata 기반 inference 검증방법: 각 repo description / README webfetch — 본 audit에선 미수행]`:
  - **TS frontend/dashboard**: storeinsight-report-engine, storecare-dashboard-{front,admin}, deepstation-web, sa-b2c-{landing,saai}, plusinsight, store-agent → `frontend-engineer` lens
  - **Python ML/data**: scapi(StoreCare ML API), deepline_rpi_hailo, seal_on_prem, seal_ds(C++ DeepStream), storeinsight-dhi → `ml-engineer` + `embedded-engineer` lens
  - **Infra/devops**: awx-playbooks, storecare-pi-gen, sd-scripts → `devops-sre` lens
  - **Docs**: storeinsight-user-guide (MDX) → `pedagogy-reviewer` (role-dict §1.2 review-전용 variant) lens
  - **Mobile**: translator_app_forwardserver (Dart) → `mobile-engineer` lens

**Role-dict §5 11 lens 매핑 결과** `[VERIFIED:Read prompts/prompt-composer-system/components/agent-role-dictionary.md:1-120 + 머리말 분류]`:
- 매핑 가능: backend-engineer / frontend-engineer / mobile-engineer / devops-sre / embedded-engineer / data-engineer / ml-engineer / ml-researcher / security-researcher / systems-researcher / hci-researcher → 11개 모두 직접 매핑 분야가 있음.
- **부재 도메인**: 명시적으로 분류 불가한 *vision/computer-vision* lens (예: deepline_rpi_hailo "single cam line counting", seal_ds "deepstream", scapi vision API). 현재 `ml-engineer + embedded-engineer`로 분리 처리 가능하나 vision-specific gotcha(DeepStream API 차이, calibration, threshold tuning)는 별도 *vision-researcher* lens가 명시적으로 필요할 수 있음. role-dict의 `[ASSUMPTION:role-dict §5에 vision-researcher 또는 computer-vision-engineer 항목 부재 — 검증방법: agent-role-dictionary.md §5 전체 read, 본 audit에선 §0-§1만 read]`. Severity: **LOW** (현재 ml-engineer/embedded-engineer 조합으로 우회 가능, but 정확도 한계 인지 필요).

**발견 사항 (2건)**:

- **F1. DeepingSource public repo 0건 — Step 2 mock 모드 진입**: composed prompt §3.3 strict 해석은 abort. 본 audit은 metadata-only로 진행하되 *결과 정확도가 외부 검증 없는 추론에 머묾*. Severity: **HIGH** (audit 결과 신뢰성 영향).
- **F2. role-dict §5 vision/CV lens 명시 부재 가능성**: deepline / seal_ds / scapi 등 vision-heavy stack을 위한 dedicated lens 부재 가능. 본 audit에선 §5 전체 미확인 — 정식 검증은 추가 read 또는 별도 audit run 필요. Severity: **LOW**.

### A.2.3 built-prompt 내용의 흐름 (§1→§7) 문제

**평가 절차**: composer Phase 1→7 + G1-G7 게이트의 분기 누락 / 데드락 / 회귀 무한 loop 여부.

**평가 근거 인용**:
- G1 (Intake) 필수 — invariant 면제 불가 (CLAUDE.md System Invariant ②). [`CLAUDE.md:52`]
- G2 cap 4 components — `optimized-prompt-composer.md:119-120`.
- Phase 5 회귀: P1-P5 fail → Phase 4 회귀 (`optimized-prompt-composer.md:264`).
- Phase 7 iteration cap: 2회. cap 도달 시 "명시적 abandon, bypass로 전환 또는 task 재정의" (`optimized-prompt-composer.md:328`).

**흐름 점검 결과**:
- ✓ G1-G6는 모두 명시. G7는 composer 본문에 **별도 정의 없음** — `prompt-evaluation-rubric.md:172` 표가 "G6 = Phase 7 post-eval, lifecycle = §5 메타 평가"로 사용하여 G7 슬롯 부재. 일부 audit composed prompt(§3.2 "router §1 catalog / §2 decision tree / §3 bypass / §7 매핑")에서 G7로 추정되는 매핑 영역이 router §7 빠른 참조 표에 해당하나 *gate* 의미의 G7은 부재. → 발견 F1.
- ✓ Phase 6 (Execution) mode A/B/C 분기 명시 (`optimized-prompt-composer.md:283-298`). 그러나 **G6은 Phase 7 post-eval의 종료 조건 (Loop 종료)** — Phase 6 *실행 자체*의 통과 게이트가 별도로 정의되지 않음. composer 본체는 "execution 산출물이 disk 또는 사용자 paste로 도착"을 가정하지만 *실행 실패 시* 어디로 회귀하는지 분기 없음 (Phase 1? bypass?).  → 발견 F2.
- ✓ trigger-prompts.md v1.2 invariant("Compose / Execute / Evaluate 세션 분리")는 강하게 명시 (`trigger-prompts.md:13-23, 119`).
- ✓ Iteration cap 2회는 명시 — 무한 loop 방어. (`optimized-prompt-composer.md:328`)
- ⚠ **회귀 경로의 mutation 정책 부재**: Phase 5 fail → Phase 4 회귀 시 이미 저장된 `.specs/<task-id>.composed.md`를 **덮어쓰기**(composer §5 산출물 저장 "최종 통과본으로 덮어쓰기", `optimized-prompt-composer.md:242`)인데, 회귀 *횟수*에 따라 이전 통과본 이력이 사라짐. 진단·blame 시 어떤 회귀에서 어떤 P-항목 fail이었는지 추적 불가. log는 누적되지만 prompt 본문 diff는 lost. → 발견 F3.

**발견 사항 (3건)**:

- **F1. G7 슬롯 부재 / G6 의미 중복**: composer 본문 §1 "Phase 사이에 명시적 gate (G1-G7)"라 명시(`optimized-prompt-composer.md:64`)했으나 G7 정의 부재. eval-rubric §7 표는 "G6 = Phase 7 + lifecycle = 분기 cycle"로 G7 매핑 없음. **모순**. Severity: **MEDIUM** (개념 일관성).
- **F2. Phase 6 실행 실패 분기 부재**: Mode A 메인 세션 실행 실패, Mode B 사용자 paste 누락, Mode C API 호출 실패 등에 대한 회귀 경로 미명시. Severity: **MEDIUM**.
- **F3. composed.md 회귀 시 덮어쓰기 — 회귀 이력 손실**: 동일 task-id로 여러 회귀가 일어나면 이전 통과본 diff 추적 불가. log는 누적되지만 prompt 본문은 1개만 유지. blame·rollback 비용 증가. Severity: **LOW** (git mtime/log로 일부 회복 가능, but `.specs/`가 `.gitignore` 처리됨 — recent commit 8859610가 ".specs directory added to gitignore for generated prompt artifacts" 시사). Severity 재평가: **MEDIUM** (gitignore 처리 시 회복 수단 0).

### A.2.4 built-prompt 내용 상충 (component 발췌 간 / Role vs Gate vs Activated)

**평가 절차**: 각 component 발췌가 §1 Role · §6 Output Gates와 모순되지 않는지 (우선순위 룰 §1+§6 > §4). 특히 `rfc-writing-template ↔ speckit-spec-generation` 상호 배타 룰이 router §2 + composer §5 발췌 가이드에서 일관 작동하는지.

**평가 근거 인용**:
- 우선순위 룰: composer §5 "모순 발견 시: §1 (Role) 또는 §6 (Output Gates)에서 우선순위 결정" (`optimized-prompt-composer.md:223`).
- rfc ↔ speckit 상호 배타: CLAUDE.md "rfc-writing-template ↔ speckit-spec-generation 상호 배타" (`CLAUDE.md:81`), router §2 "키워드 우선" (`prompt-component-router.md:64-66`), speckit §0 metadata "충돌 가능: rfc-writing-template 동시 활성 금지" (`speckit-spec-generation.md:22`), router §7 "rfc-writing-template은 BYPASS" (`prompt-component-router.md:202-203`).

**상충 점검 결과**:
- ✓ rfc ↔ speckit: 4개 SSOT(CLAUDE.md, router §2, speckit §0, router §7)에서 일관 명시. PASS.
- ⚠ **`layer` 필드 라이브러리 내 일관성 부재**: router §1.1 (`prompt-component-router.md:46`)이 `layer`를 SSOT 메타로 선언 — `조합 인프라` / `분석 컨텐츠` / `domain content` 3축. 그러나 router §1 표(`prompt-component-router.md:25-38`)는 *layer 컬럼이 없다* — `last_modified` / `version` / `owner` / `layer`를 §1.1에서 "의무"로 선언하면서 §1 표에 노출하지 않음. 게다가 7 component 중 6개(`agent-role-dictionary`, `multi-agent-analysis-template`, `code-review-rubric`, `experiment-design-template`, `rfc-writing-template`, `autonomous-optimization-loop`)의 `## 0. Router 등록 metadata` 표에 **`layer` 행 부재** — speckit만 명시. → 발견 F1.
- ⚠ **CLAUDE.md "도메인 content" 라이브러리 카운트 ↔ optimized-prompt-composer.md "시리즈 구성" 카운트 mismatch 가능**: CLAUDE.md "domain content(도메인 특화)"는 `code-review-rubric / experiment-design-template / rfc-writing-template / autonomous-optimization-loop / speckit-spec-generation` 5개 — 일관. composer 머리말 "시리즈 구성"은 1~12 번호로 12개 (composer 자체 포함). PASS.
- ⚠ **`trigger.md`가 어디에도 등록 안 됨**: CLAUDE.md "컴포넌트 라이브러리" 절 미등록. router §1 catalog 미등록. composer "시리즈 구성" 미등록. 그럼에도 파일은 존재 + repo에 commit. → 발견 F2 (A.2.1-F1과 중복 부분 — 손상 vs 등록은 다른 시각).
- ⚠ **trigger-prompts.md v1.2 invariant ↔ composer §7 Phase 6 mode A "메인 세션 직접 실행"**: composer §7 Mode A는 "composed prompt를 다음 user message로 발송" (`optimized-prompt-composer.md:284`) — *같은 세션* 실행을 허용하는 표현. 그러나 trigger-prompts.md v1.2는 "Compose / Execute / Evaluate 세션 분리"를 invariant로 격상 (CLAUDE.md System Invariant ①) → composer §7 Mode A는 *원칙적으로 금지*. 단 mode A를 "Composer가 끝난 후 다음 conversation에서 메인 세션을 시작"으로 해석할 수 있으나 본문 표현이 모호. → 발견 F3.
- ⚠ **composer §0 Skill frontmatter ↔ trigger-prompts.md 방법 B default 모드 mismatch 가능성**: composer §0 SKILL.md frontmatter는 "rough request → composed prompt"로 광범위 trigger description (`optimized-prompt-composer.md:39-48`). trigger-prompts.md 방법 B "default 실행 모드는 compose-only auto (A-2)"는 명시되지만 *frontmatter 자체에 default 모드 식별자 없음*. → 발견 F4 (LOW).

**발견 사항 (4건)**:

- **F1. `layer` 필드 component metadata 6/7 누락**: router §1.1이 의무 선언했으나 6개 component(`agent-role-dictionary`, `multi-agent-analysis-template`, `code-review-rubric`, `experiment-design-template`, `rfc-writing-template`, `autonomous-optimization-loop`)의 `## 0. Router 등록 metadata` 표에 `layer` 행 부재. Severity: **HIGH** (System Invariant ④ "레이어 분리 유지"의 기계적 강제력 약화).
- **F2. `trigger.md` library 등록 0건**: 파일 존재하나 router/composer/CLAUDE.md 어디에도 등록 없음. ghost file. Severity: **MEDIUM**.
- **F3. composer §7 Mode A ↔ Invariant ① 모호성**: "같은 세션 실행" 해석 여지. Severity: **MEDIUM**.
- **F4. composer §0 frontmatter에 default 모드 식별자 부재**: trigger-prompts.md A-2 default를 SKILL.md에서 자동 활성화하는 메커니즘 약함. Severity: **LOW**.

---

## A.3 3 test built-prompt 모의 실행 결과

> ⚠ 본 모의는 *Composer Phase 1→5 trace만* — 실제 Phase 6 dispatch 0건. spec.E echo, routing log, 발췌 ref는 작성하되 *실행 결과는 생성하지 않음* (System Invariant ① 준수).

### A.3.1 Test repo 선정 + 이유

`[VERIFIED:bash gh repo list DeepingSource @2026-05-26]` 30개 metadata에서 *언어 + 도메인* 이질성 기준으로 3개 선정:

| ID | repo | 언어 | 추정 도메인 | 본 audit의 rough request |
|---|---|---|---|---|
| **T1** | `seal_on_prem` | Python | on-prem ML deployment + 보안 (description "SEAL on Premise") | "신규 customer의 SEAL on-prem 환경 등록 방식 결정(Teleport / VPN / SSH bastion 중)" |
| **T2** | `deepline_rpi_hailo` | Python | edge vision (description "DeepLine for RPi with hailo - single cam line counting") | "RPi+Hailo single-cam line counting 정확도 개선 — threshold tuning vs ONNX 양자화 vs fine-tuning 자동 탐색" |
| **T3** | `storeinsight-user-guide` | MDX | docs | "StoreInsight User Guide v2 — 5 feature + onboarding 흐름을 외부 customer 대상 design doc 형태로 작성" |

**이질성 검증**: T1=Decision+devops/security, T2=Analysis+autonomous-optimization, T3=Generation+rfc — 3개의 router 분기가 *모두 다름*. composer routing 다양성을 cover.

### A.3.2 Mock composed prompt trace (Phase 1→5)

#### T1 — `seal-onprem-deploy-method-decision`

**Phase 1 (Intake)** `[mock — 본 audit run 안에서 trace만, .specs 파일 생성 0]`:
```
A. Purpose: 신규 customer의 SEAL on-prem 환경에 적합한 서버 등록 방식 1개 채택.
B. Output: Decision
C. Scale: 3 topic (Teleport / VPN / SSH bastion)
D. 시간 1일, 한국어 markdown, 보안 critical
E. 성공: (a) 각 방식 위협 모델 명시 (b) 운영 부담(on-call, key rotation) 비교 (c) 1주 PoC 가능 actionability
G. Reversibility: [COSTLY-TO-REVERSE]
L. Domain: security-researcher(1), devops-sre(2), backend-engineer(3)
```
✓ G1 통과 (A-E 충족, single goal, 측정 가능 criteria).

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-26T mock] task=seal-onprem-deploy-method-decision
- selected: [task-spec@1.0, multi-agent-template@1.x(§2+§4+§7+§8), role-dict@1.x(security-researcher×devils-advocate; devops-sre×risk-auditor; backend-engineer×proposer), eval-rubric@1.x]
- bypass: none
- domain lens: [security-researcher, devops-sre, backend-engineer] (source: spec.L)
- token estimate: 28k / cap 50k
- multi-LLM: COSTLY-TO-REVERSE → 검토 후보 (실제 활성은 spec.K 미명시로 default off)
- rationale: 3 topic + Decision + security critical + COSTLY-TO-REVERSE → multi-agent 동일 criteria 평가 + 보안 lens 비판 강도 필요
```
✓ G2 통과 (cap 4 이내, log 작성, bypass 미해당).

**Phase 3 (Context)**: Context Manifest 작성 — spec.F audience(team 내부 + 인프라 결정자) Header, CLAUDE.md project conventions Header, 과거 SEAL onprem PR/issue Reference(있는 경우, 본 mock에선 없음).
✓ G3 통과.

**Phase 4 (Assembly)**: composed prompt §1-§7 구조 작성. multi-agent-template §2 framework + §4 workflow + §7 gates 발췌, role-dict §5.9 security-researcher + §5.4 devops-sre + §5.1 backend-engineer 발췌. §6 Output Gates에 spec.E echo.
⚠ G4 — **모의에선 `.specs/T1.composed.md` 저장 안 함** (본 audit run에서 부산물 생성 회피, composed prompt §3.3 명시 — 합법).

**Phase 5 (Pre-validation)**: P1-P5 검사 모의.
- P1 spec coverage: A-E 키워드 모두 §1-§6에 echo. PASS.
- P2 output 형식: 한국어 markdown 명시. PASS.
- P3 gate: §6 spec.E echo + multi-agent G0-G4 gate ref. PASS.
- P4 충돌: §1 Role(보안 critical 톤) ↔ §4 다 모순 없음. PASS.
- P5 redaction: customer 이름 미명시(mock), 실제 run에선 redact 필요. PASS for mock.
✓ G5 통과.

#### T2 — `deepline-rpi-hailo-linecount-tuning`

**Phase 1 (Intake)** `[mock]`:
```
A. Purpose: deepline_rpi_hailo의 single-cam line counting metric을 자동 탐색으로 개선.
B. Output: Analysis (단일 스칼라 지표 자동 반복 최적화)
C. Scale: 단일 metric (예: F1 또는 counting accuracy), trial 횟수 budget 별도
D. Hailo+RPi 환경, REVERSIBLE (commit/revert 가능)
E. 성공: baseline 대비 metric ≥ X% 개선, trial 로그(results.tsv) + best commit + 학습 메모
G. Reversibility: [REVERSIBLE]
L. Domain: ml-engineer(1), embedded-engineer(2), systems-researcher(3)
```
✓ G1 통과.

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-26T mock] task=deepline-rpi-hailo-linecount-tuning
- selected: [task-spec@1.0, autonomous-optimization-loop@1.0(§3+§4+§6), role-dict@1.x(systems-researcher×proposer; ml-engineer×devils-advocate; embedded-engineer×evidence-checker)]
- bypass: none
- domain lens: [ml-engineer, embedded-engineer, systems-researcher]
- token estimate: ~18k / cap 50k (실비용은 trial × measure 지배)
- multi-LLM: off
- rationale: 단일 스칼라 지표 + 자동 측정 가능 + REVERSIBLE → autonomous-optimization-loop 명확 분기. experiment-design은 best 발견 후 *검증* 단계에서 순차.
```
✓ G2 통과. **bypass된 component**: multi-agent-template(자동 탐색이라 대안 비교 아님), experiment-design(검증 단계 아님 — 탐색 우선), rfc-writing(B=Analysis 아님), code-review(B=Review 아님), speckit(B=Generation 아님).

**Phase 3-5 (요약)**:
- §4 Activated Components: autonomous-optimization §3 Step 0-5 + §4 Quality Gates + §6 anti-patterns 발췌.
- §6 Output Gates: results.tsv format + best commit hash + 학습 메모(memory durable pattern) + baseline 대비 % 명시.
- P1-P5 PASS. ⚠ P6 token blowup 위험은 trial 횟수에 비례 — composer prompt 자체는 small이나 *실비용*은 측정 명령에 dominate. 본 audit은 prompt cost만 평가.

#### T3 — `storeinsight-user-guide-v2-design`

**Phase 1 (Intake)** `[mock]`:
```
A. Purpose: StoreInsight User Guide v2 — 5 feature + onboarding 흐름 design doc 작성.
B. Output: Generation
C. Scale: 5 sub-section + onboarding flow = ~6 unit, 2-3 페이지 한국어 markdown
D. 외부 customer 대상, 한국어 + 영문 기술 용어 병기
E. 성공: 각 feature 1단락 설명 + 캡처 placeholder + onboarding flow diagram ref + reviewer feedback round 1회 통과
G. Reversibility: [REVERSIBLE] (문서이므로)
F. Audience: 외부 customer (정확히는 StoreCare 도입 신규 client)
L. Domain: pedagogy-reviewer(1), frontend-engineer(2)
```
✓ G1 통과.

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-26T mock] task=storeinsight-user-guide-v2-design
- selected: [task-spec@1.0, rfc-writing-template@1.0(§3+§4), role-dict@1.x(pedagogy-reviewer×proposer; consistency-checker)]
- bypass: speckit-spec-generation (키워드 미일치 — spec.A에 spec/plan/tasks/SDD 없음, *user guide design doc*은 자유 형식), multi-agent-template (단일 design doc, multi-topic 아님)
- domain lens: [pedagogy-reviewer, frontend-engineer]
- token estimate: ~16k / cap 50k
- multi-LLM: off
- rationale: Generation + external audience + design doc → rfc-writing-template. speckit과 *상호 배타* — router §2 키워드 우선 분기로 rfc 채택.
```
✓ G2 통과. **rfc ↔ speckit 상호 배타 룰이 작동함**.

**Phase 3-5 (요약)**:
- §4 Activated Components: rfc §3 Step 0-6 + §4 Quality Gates 발췌.
- §6 Output Gates: spec.E echo + reviewer feedback log + 캡처 placeholder + 외부 customer 친화 톤.
- P1-P5 PASS. P7 bypass 재검토: design doc 1개는 composition value 있음(외부 audience, 합의 필요) → 진행.

### A.3.3 평가 매트릭스 — prompt-evaluation-rubric §1 P1-P7 + §2 smell

> 3 mock built-prompt 자체에 대한 평가. 본 audit auditor가 작성·평가 → `[self-eval 경고]` 부착 (composed prompt §3.3 명시).

| Test | P1 spec cov | P2 output 형식 | P3 gate | P4 충돌 | P5 redaction | P6 토큰 | P7 bypass 재검토 | smell 2.1 Redund | smell 2.2 Contra | smell 2.3 Vague gate | smell 2.4 Missing fallback | smell 2.5 Anchoring | smell 2.6 Over-scoping |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **T1** | ✓ | ✓ | ✓ | ✓ | ✓ (mock) | ✓ 28k | ✓ multi-agent 가치 있음 | ⚠ multi-agent §7 gates ↔ rubric P3 일부 중복 | ✓ | ✓ | ⚠ baseline 읽기 실패 fallback 명시 필요 (template §3.1 C-016) | ✓ | ✓ |
| **T2** | ✓ | ✓ | ✓ | ✓ | n/a (no PII) | ⚠ prompt cost OK, 실비용 trial×measure | ✓ 단일 metric 자동 탐색 — composition 가치 명확 | ✓ | ✓ | ✓ G0-G5 명시 | ⚠ measure 실패 시 fallback 명시 권장 (autonomous §3 Step 0) | ✓ | ✓ |
| **T3** | ✓ | ✓ | ✓ | ✓ | ✓ (외부 audience이므로 customer 이름 한정) | ✓ ~16k | ✓ external + 합의 → rfc 가치 | ✓ | ✓ | ✓ rfc §4 quality gates | ⚠ reviewer 응답 missing 시 fallback | ✓ | ✓ |

**[self-eval 경고]** — 본 매트릭스는 auditor가 작성·자가 채점. 신뢰도 보장 위해 별도 LLM(gemini-bridge / codex-bridge) 또는 별도 Claude 세션에서 blind A/B로 재채점 권고. self_upgrade §B4 정책 준수.

### A.3.4 Synthesis — 3 test 공통 약점

multi-agent-analysis-template §4 synthesis 단계 모의 적용:

**공통 약점 1단락**:
> 3 mock built-prompt 모두에서 `smell 2.4 Missing fallback`이 ⚠ 단계로 검출됨 — T1은 baseline.md 읽기 실패, T2는 measure command 실패, T3는 reviewer 응답 missing 시의 fallback 절차가 composer §5 Phase 4 발췌에 *자동 포함되지 않음*. multi-agent-template §3.1 C-016 ("파일을 읽지 못하면 즉시 abort"), autonomous-optimization §3 Step 0("measure 실패 → revert"), rfc §3("reviewer round skip 정책")이 각각 component-local로 명시되어 있으나 **composer §5 Phase 4 발췌 가이드(`optimized-prompt-composer.md:227-235`)에는 "fallback 명시" 항목이 없다**. 발견 사항: 각 component의 *failure-mode fallback* 룰을 composer 발췌 가이드에 횡단적으로 명시하는 boilerplate가 필요. **Severity: MEDIUM** (개별 component 정독 시 발견되지만 composer 단독 사용자가 놓치기 쉬움).

---

## A.4 발견 사항 priority 표

각 항목에 *재현 절차 + 수정 권고 + 파일 위치*.

### BLOCKING (0건)

없음.

### HIGH (2건)

| ID | 발견 | 재현 절차 | 수정 권고 | 파일 |
|---|---|---|---|---|
| H1 | A.2.4-F1: 6/7 component metadata에 `layer` 필드 부재 | `grep "layer" prompts/prompt-composer-system/components/*.md` → speckit만 hit | 6 component(`agent-role-dictionary`, `multi-agent-analysis-template`, `code-review-rubric`, `experiment-design-template`, `rfc-writing-template`, `autonomous-optimization-loop`)의 `## 0. Router 등록 metadata` 표에 `layer` 행 추가. 값은 CLAUDE.md "컴포넌트 라이브러리" 분류에 1:1 매핑. | 각 components/*.md §0 |
| H2 | A.2.2-F1: DeepingSource public repo 0건 → audit Step 2 정확도 한계 | `gh repo list DeepingSource --visibility public` 0건 | 본 audit 정책 결정 필요: (a) public 0건일 때 abort, (b) metadata-only inference + 정확도 한계 명시, (c) `gh repo view <internal>` 1-2개 사용자 승인 하에 허용. 결정을 composed prompt §3.3 또는 audit-composer-system.md (Part B)에 명시. | `.specs/composer-system-audit.composed.md`, Part B |

### MEDIUM (6건)

| ID | 발견 | 재현 절차 | 수정 권고 | 파일 |
|---|---|---|---|---|
| M1 | A.2.1-F1: `trigger.md` 표준 머리말 위반 | `head -3 prompts/prompt-composer-system/trigger.md` → 머리말 없음 | 표준 3필드 머리말 추가 + "아래 구분선부터가 복사 본문임" 명시 (CLAUDE.md:28 규약 echo) | `prompts/prompt-composer-system/trigger.md` |
| M2 | A.2.1-F2: trigger.md ↔ trigger-prompts.md A-2 DRY 위반 (drift 위험) | `diff <(sed -n '91,122p' trigger-prompts.md) trigger.md` → 본문 유사 | 선택 1: trigger.md 본문을 trigger-prompts.md A-2의 컨텐츠 generation 스크립트로 만들기. 선택 2: trigger.md에 "이 파일은 trigger-prompts.md A-2의 paste-ready 사본 — 정본 수정 시 동기화 필요" 1줄 명시. 선택 3: trigger.md 삭제 + 사용자에게 trigger-prompts.md A-2 paste 안내. | trigger-prompts.md / trigger.md |
| M3 | A.2.3-F1: G7 슬롯 부재 / G6 의미 중복 | `optimized-prompt-composer.md:64` "G1-G7" 선언 ↔ §8 Phase 7 의 Gate ID = G6 → G7 정의 누락 | 옵션 A: G1-G6로 통일 + Phase 7 종료 게이트는 G6, lifecycle 메타 평가는 별도 gate 없음 명시. 옵션 B: G7을 Phase 7 종료 + G6는 Phase 5 pre-validation (현재 명명과 swap). | composer §1, §6, §8 + eval-rubric §7 |
| M4 | A.2.3-F2: Phase 6 실행 실패 분기 부재 | composer §7 Mode A/B/C 본문 검토 → 실패 처리 없음 | composer §7 끝에 "실행 실패 시: Mode A는 Phase 4로 회귀(prompt 발췌 보강), Mode B는 사용자 paste 누락 보고, Mode C는 API 오류 분기" 한 단락 추가 | composer §7 |
| M5 | A.2.3-F3: composed.md 회귀 시 덮어쓰기 — 회귀 이력 손실 (gitignore 처리 시 회복 0) | recent commit 8859610로 `.specs/`가 `.gitignore` 처리됨 → 회귀별 prompt diff lost | 옵션 A: `.specs/<task-id>.composed.<iter-N>.md`로 버전 suffix. 옵션 B: composer log에 prompt diff 첨부 (token cost 증가). 옵션 C: 현 상태 유지 + log에 회귀 이유만 누적 (현 default — but 위험 명시). | composer §5 산출물 저장 |
| M6 | A.2.4-F2: `trigger.md` library 등록 0건 (ghost file) | `grep "trigger.md" CLAUDE.md optimized-prompt-composer.md prompt-component-router.md` → CLAUDE.md:28만 hit (규약 예시), 등록 0건 | CLAUDE.md "컴포넌트 라이브러리" 또는 "인터페이스" 절에 trigger.md 명시 (역할: A-2 paste-ready 본문 — trigger-prompts.md A-2의 short-form). M2와 동시 처리 권장. | CLAUDE.md "컴포넌트 라이브러리" 절 |
| M7 | A.3.4-Synthesis: composer §5 Phase 4 발췌 가이드에 *failure-mode fallback* 횡단 boilerplate 부재 | 3 mock test 모두에서 smell 2.4 ⚠ | composer §5 "발췌 가이드" 또는 "Component 발췌 룰"에 "각 component에서 *failure-mode fallback 절*도 함께 발췌" 1줄 추가 | composer §5 |

(M6과 M2는 함께 처리되면 1 PR으로 묶을 수 있음 — A.5에서 결정)

### LOW (3건)

| ID | 발견 | 수정 권고 | 파일 |
|---|---|---|---|
| L1 | A.2.1-F3: spec.E echo 명시 게이트 부재 (G4가 P1으로 대체 가능하나 회귀 1회 비용) | G4 체크리스트에 "spec.E의 각 criterion이 §6에 grep 단위로 echo" 항목 추가 | composer §5 Gate G4 |
| L2 | A.2.2-F2: role-dict §5 vision/CV lens 부재 가능 | role-dict §5 전체 read 후 별도 audit (본 run에선 §0-§1만 read) | role-dict §5 |
| L3 | A.2.4-F4: composer §0 frontmatter에 default 모드 식별자 부재 | SKILL.md frontmatter description에 "default: A-2 compose-only auto (trigger-prompts.md A-2 참조)" 1줄 echo | composer §0 |

---

## A.5 self_upgrade "1회 1대상" 단위 PR 제안

> `self_upgrade.md §0` "1회 실행 = 대상 프롬프트 1개" + "작은 diff" + "사람 승인 게이트" 준수. 각 PR이 review-가능한 단일 변경.

### PR 1 — `feat(prompts): add layer field to 6 component §0 metadata` [HIGH, H1]
- 변경: 6 component의 `## 0. Router 등록 metadata` 표에 `layer` 행 추가.
- diff: 6 파일 × 1줄 = 6줄.
- 검증: `grep "layer:" prompts/prompt-composer-system/components/*.md | wc -l` → 7 (현재 1).
- 출처: `[VERIFIED:read prompts/prompt-composer-system/prompt-component-router.md:46 @2026-05-26]` (§1.1 의무 선언).

### PR 2 — `fix(prompts/trigger.md): add standard header + decouple from A-2 drift` [MEDIUM, M1+M2+M6 묶음]
- 변경 (소): trigger.md 머리말 추가 + "trigger-prompts.md A-2의 paste-ready 사본임" 명시 + CLAUDE.md "인터페이스" 절에 trigger.md 1줄 등록.
- 또는 변경 (대): trigger.md 삭제 + 사용자 README 안내 변경 → 이는 별도 PR으로 격상 권고.
- self_upgrade 룰: 1 PR 1 파일 원칙 — 본 PR은 trigger.md + CLAUDE.md 2 파일이라 *small bundled diff*로 분류 (M1+M2+M6은 의미상 1 fix).
- 출처: `[VERIFIED:read prompts/prompt-composer-system/CLAUDE.md:28 @2026-05-26]`.

### PR 3 — `docs(composer): clarify G7 slot — unify G1-G6 + lifecycle separate` [MEDIUM, M3]
- 변경: composer §1, §6 표, eval-rubric §7 표에서 G7 표현을 G6로 통일. lifecycle 메타 평가는 "(분기 cycle)" 슬롯으로 별도 명시.
- diff: 3 파일 × 1-3줄.
- 검증: `grep "G7\|G1-G7" prompts/prompt-composer-system/*.md` → 0.

### PR 4 — `docs(composer): add Phase 6 execution failure routing` [MEDIUM, M4]
- 변경: composer §7 끝에 mode-별 실패 회귀 1단락 추가.
- diff: 1 파일 × ~10줄.

### PR 5 — `docs(composer): preserve regression history of composed.md` [MEDIUM, M5]
- 변경: composer §5 산출물 저장 규약에 회귀 버전 suffix(`<task-id>.composed.iter-<N>.md`) 또는 log 첨부 정책 명시. 옵션 결정은 사용자 선택.
- diff: 1 파일 × ~5줄.

### PR 6 — `docs(composer): require failure-mode fallback in component excerpts` [MEDIUM, M7]
- 변경: composer §5 "발췌 가이드" 또는 "Component 발췌 룰"에 "fallback 절도 발췌" 1줄 추가.
- diff: 1 파일 × 1줄.

### PR 7 — Part B 신규 파일 + Part C 1줄 추가 [본 audit run의 직접 산출]
- 변경: `prompts/prompt-composer-system/audit-composer-system.md` 신규 + CLAUDE.md 1줄 + (README.md 처리는 별도 결정).
- diff: 1 신규 + 1 줄 (CLAUDE.md).
- 사람 승인 게이트: **본 audit Step 5에서 승인 받은 후 mutation**.

### LOW PR들 (배치 가능)
- PR 8: L1 G4 spec.E echo grep 추가 (1줄).
- PR 9: L3 composer §0 default 모드 echo (1줄).
- PR 10: L2는 별도 audit run으로 (role-dict §5 vision lens) — 본 audit과 분리.

**총 7 high/medium PR + 3 low PR** — 각각 1회 1대상 + 작은 diff. self_upgrade §B4 "독립 채점 권장" 적용: PR 1, PR 2, Part B는 블라인드 채점 권장 (다른 LLM 또는 별도 세션).

---

## A.6 출처 (tier별)

### Tier 1 (1차 SSOT — repo 내부 정본)
- `prompts/prompt-composer-system/CLAUDE.md` (System Invariants + library 분류) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/optimized-prompt-composer.md` (Phase 1-7 orchestrator) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/prompt-component-router.md` (§1 catalog + §1.1 layer 의무 + §2 decision tree + §7 매핑) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/prompt-evaluation-rubric.md` (§1 P1-P7 + §2 smell + §3 5축 + §4 mapping) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/trigger-prompts.md` (v1.2 invariant + A-1/A-2/A-3 + C-6/C-7) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/task-spec-template.md` (A-E 필수 + F-J 권장 + K-L 선택) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/context-injection-patterns.md` (7-source taxonomy + 4 patterns + redaction protocol) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/component-discovery-ledger.md` (RCA-001, SKIT-001 등 status table) `[VERIFIED:Read @2026-05-26]`
- `prompts/prompt-composer-system/components/agent-role-dictionary.md` §0-§1.4 `[VERIFIED:Read limit=120 @2026-05-26]`
- `prompts/prompt-composer-system/components/multi-agent-analysis-template.md` §0-§1 `[VERIFIED:Read limit=100 @2026-05-26]`
- `prompts/prompt-composer-system/components/code-review-rubric.md` §0-§1.1 `[VERIFIED:Read limit=40 @2026-05-26]`
- `prompts/prompt-composer-system/components/experiment-design-template.md` §0-§1.1 `[VERIFIED:Read limit=40 @2026-05-26]`
- `prompts/prompt-composer-system/components/rfc-writing-template.md` §0-§1.3 `[VERIFIED:Read limit=60 @2026-05-26]`
- `prompts/prompt-composer-system/components/autonomous-optimization-loop.md` §0-§1.1 `[VERIFIED:Read limit=40 @2026-05-26]`
- `prompts/prompt-composer-system/components/speckit-spec-generation.md` (전문) `[VERIFIED:Read @2026-05-26]`
- `self_upgrade.md` (Loop A/B 거버넌스 + §B4 독립 채점) `[VERIFIED:Read @2026-05-26]`
- `README.md` (repo root — prompts-pack, worklog 안내) `[VERIFIED:Read @2026-05-26]`

### Tier 2 (외부 — gh CLI)
- `[VERIFIED:bash gh repo list DeepingSource --limit 30 --json ... @2026-05-26]` 30개 repo metadata. 모두 PRIVATE/INTERNAL.

### Tier 3 (추정 — 검증 미수행)
- `[ASSUMPTION:DeepingSource repo description+language에서 도메인 분류 — 검증방법: 각 repo README webfetch 또는 사용자 confirm]`
- `[ASSUMPTION:role-dict §5에 vision/CV-engineer 항목 부재 — 검증방법: agent-role-dictionary.md §5 전체 read]`
- `[ASSUMPTION:.specs/ gitignore 처리 — 검증방법: cat .gitignore 또는 commit 8859670 본문 확인 (본 audit run에선 미확인, 메모리 기반)]`

---

## A.7 위험 / 보류 사항

- **[self-eval 경고]** 본 audit의 모든 점수·매트릭스·priority는 메인 세션 자기 진단. self_upgrade §B4 정책에 따라 *블라인드 독립 채점* 권고 — 별도 LLM(`codex-bridge` / `gemini-bridge`) 또는 별도 Claude 세션에 본 보고서 + composed prompt를 블라인드 입력으로 전달하여 5축 재채점.
- **DeepingSource public repo 0건** — Step 2 정확도 한계. metadata-only inference가 합당한지 user 결정 필요(H2 권고).
- **본 audit run에서 mutation 0건** (composed prompt §3.3 strict 준수) — 본 보고서 Write + audit prompt 디렉토리 mkdir만 read-only 산출로 분류. Part B 신규 파일 / Part C 1줄 추가는 **Step 5 사용자 승인 후만** 진행.
- **role-dict §5 vision lens 부재 가능성**은 본 audit에서 §5 전체 미확인 — L2로 격하, 별도 audit run.
- **DeepingSource repo 모두 private/internal**이라 audit composed prompt §3.3 strict 해석은 *즉시 중단*. 본 audit은 metadata만 사용·내용 fetch 0건이므로 약화된 해석을 채택했음을 명시. user 의견 다르면 H2 결정에 맞춰 audit 재실행.

---

## A.8 메타 — 본 audit의 한계

1. **components/multi-agent-analysis-template.md 부분 read만**: 61KB 중 §0-§1 (100줄)만 read. §2 framework / §3 contracts / §4 workflow / §7 gates / §8 anti-patterns 전체는 미확인 — 본 audit의 4-checklist는 *composer 발췌 가이드와 §1 머리말만으로* 평가했고, 실제 multi-agent 내부 룰의 일관성은 추가 audit 필요.
2. **agent-role-dictionary.md 부분 read만**: 33KB 중 §0-§1.4 (120줄)만 read. §2 critique-roles / §5 domain roles 전체는 미확인.
3. **component-discovery-{collect,approve}.md 미확인**: ledger만 read.
4. **본 audit이 *자기 자신*도 점검 대상**(composed prompt §3.2 Part B 요구 — "audit prompt가 *자기 자신*도 점검 대상에 포함해야 함을 §3에 명시")이지만, *자기 점검은 다른 LLM 권고*. 본 보고서는 self-진단 → blind external review 권고.

---

*보고서 끝. Part B 초안과 Part C diff안은 본 audit run의 메인 응답에 인라인 출력 (저장은 승인 게이트 통과 후).*
