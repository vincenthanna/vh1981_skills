# Composer System Audit — 2026-05-29

> **무엇**: `prompts/prompt-composer-system/` 자체를 정기 진단하는 audit 보고서 (2회차). composer Phase 1-7 (G1-G7)을 거치지 않고, audit prompt 자체를 별도 layer(거버넌스·메타 진단 도구 — 4번째 축)에서 실행.
> **용도**: 시스템 변경 후 정기 점검. 발견 사항을 `self_upgrade.md` "1회 1대상" 단위 PR 제안으로 분리. 직전 audit(2026-05-26) 대비 Δ 확인 + 반복 발견 BLOCKING 격상.
> **시리즈 위치**: 독립 실행 — composer Phase 밖, build 흐름에 호출 금지. (System Invariant ① 준수)
>
> Audit 입력: `prompts/prompt-composer-system/audit-composer-system.md` (Conv2 직접 실행 — Conv1 compose 별도 진행 안 함, audit prompt 정형이므로 합법)
> Audit 실행: Conv2 (본 conversation, 2026-05-29) — Phase 6 실 dispatch 0, 모의 trace만.
> Audit 평가: Conv3 권고 (별도 LLM 또는 별도 세션 블라인드 채점)

---

## 0. Audit run 메타

- Auditor: claude-opus-4-7 (1M context), Claude Code 메인 세션 (`/Users/yeonhuigim/workspace/vh1981_skills`)
- Branch: `main`
- Working tree: clean (audit run에서 read-only 작업 + 본 보고서 Write만)
- `[VERIFIED:bash gh repo list DeepingSource --visibility public --limit 30 @2026-05-29]` public repo **2건** (직전 audit 2026-05-26: 0건) — H2 RESOLVED.
- `[ASSUMPTION:self-eval audit]` 본 audit은 메인 세션 자기 진단이므로 self-eval bias 위험 — `[self-eval 경고]` 부착. 블라인드 독립 채점은 §A.7 권고에 명시.
- **반복 발견 처리**: §5.2 룰에 따라 동일 발견 ID가 2026-05-26 보고서에 명시되어 있고 현 run에서 PASS 못 받으면 **BLOCKING으로 격상**.

---

## A.1 시스템 개관 — 인벤토리 표 + Δ from 2026-05-26

### A.1.1 `prompts/prompt-composer-system/` (root) — 11 파일 → 12 파일

| # | 파일 | layer | Δ from 2026-05-26 |
|---|---|---|---|
| 1 | `CLAUDE.md` | 진입 규약 | 변경 없음 (디렉토리 규약 SSOT) |
| 2 | `optimized-prompt-composer.md` | 조합 인프라 | 변경 없음 (M3-M7 미반영) |
| 3 | `task-spec-template.md` | 조합 인프라 | 변경 없음 |
| 4 | `prompt-component-router.md` | 조합 인프라 | 변경 없음 |
| 5 | `context-injection-patterns.md` | 조합 인프라 | 변경 없음 |
| 6 | `prompt-evaluation-rubric.md` | 조합 인프라 | 변경 없음 |
| 7 | `trigger-prompts.md` | 인터페이스(entry) | 변경 없음 |
| 8 | `trigger.md` | 인터페이스(entry) | **M1/M2/M6 미해결** (머리말 0, 등록 0) |
| 9 | `component-discovery-collect.md` | 라이브러리 성장 (메타) | 변경 없음 |
| 10 | `component-discovery-approve.md` | 라이브러리 성장 (메타) | 변경 없음 |
| 11 | `component-discovery-ledger.md` | 라이브러리 성장 (공유 상태) | 변경 없음 |
| 12 | **`audit-composer-system.md` (신규)** | 진단 도구 (4번째 축) | **2026-05-26 PR 7로 추가됨** — N1 fix 1회 적용 (`7b97ef1`) |

### A.1.2 `prompts/prompt-composer-system/components/` (7파일) — `layer` 필드 echo 진척

| # | 파일 | layer (CLAUDE.md 분류) | `\| layer \|` echo 여부 | Δ from 2026-05-26 |
|---|---|---|---|---|
| 1 | `agent-role-dictionary.md` | 분석 컨텐츠 | **✗ 미echo** | 변경 없음 (`## 0. Router 등록 metadata` section 자체 없음) |
| 2 | `multi-agent-analysis-template.md` | 분석 컨텐츠 | **✗ 미echo** | 변경 없음 (`## 0. Router 등록 metadata` section 자체 없음) |
| 3 | `code-review-rubric.md` | domain content | ✓ echo | **commit `1643ad8` (H1 partial)** |
| 4 | `experiment-design-template.md` | domain content | ✓ echo | **commit `1643ad8` (H1 partial)** |
| 5 | `rfc-writing-template.md` | domain content | ✓ echo | **commit `1643ad8` (H1 partial)** |
| 6 | `autonomous-optimization-loop.md` | domain content | ✓ echo | **commit `1643ad8` (H1 partial)** |
| 7 | `speckit-spec-generation.md` | domain content | ✓ echo | 변경 없음 (이전부터 보유) |

**관찰**: 7 component 중 5개가 `layer` 필드 보유 (직전 audit 1개 → +4). 잔여 2개(`agent-role-dictionary`, `multi-agent-analysis-template`)는 *분석 컨텐츠* layer로 분류되지만 `## 0. Router 등록 metadata` section 자체가 존재하지 않음 → 단순 1행 추가가 아니라 *section 신설*이 필요. H1 PARTIAL 잔여 — §5.2 반복 발견 룰에 따라 **BLOCKING 격상**. → 발견 B0.

### A.1.3 repo root 보조 파일

| 파일 | 존재 | Δ from 2026-05-26 |
|---|---|---|
| `README.md` | ✓ | 변경 없음 (prompt-composer-system 절 없음 — A.4-F1 미해결, severity LOW) |
| `self_upgrade.md` | ✓ | 변경 없음 |
| `self_upgrade-changelog.md` | ✓ | 1줄 추가 (2026-05-26 speckit 신규 entry) |

---

## A.2 Checklist 4항목 평가 (재실행)

### A.2.1 built-prompt가 rough request를 손상시키는가?

**§3.1 자동 검사 결과** `[VERIFIED:bash for f in ...; do head -3 "$f" | grep -q "^> \*\*무엇\*\*" || echo MISSING; done @2026-05-29]`:
- MISSING_HEADER: `prompts/prompt-composer-system/CLAUDE.md`
- MISSING_HEADER: `prompts/prompt-composer-system/trigger.md`

**발견 사항 (2건)**:

- **B1 (격상: HIGH M1→BLOCKING). `trigger.md` 표준 머리말 위반 — 반복 발견**: 2026-05-26 A.2.1-F1과 동일. CLAUDE.md:28 규약 "trigger.md처럼 통째로 복사해 쓰는 prompt 파일도 머리말을 붙이되, '아래 구분선부터가 복사 본문'임을 명시한다"가 여전히 미준수. `trigger.md:1` 첫 줄이 `너는 지금부터 optimized-prompt-composer로 동작한다.` — 머리말 0. **§5.2 반복 발견 격상 룰 적용**. Severity: **BLOCKING(escalated)**.
- **N1 (신규). `CLAUDE.md` 자기 규약 위반**: CLAUDE.md:1-5가 "이 디렉토리(하위 `components/` 포함)의 모든 `*.md` 문서는 표준 머리말로 시작한다"고 선언하면서 정작 자기 자신은 표준 3필드 머리말이 없음. **자기-참조 모순**. 단, CLAUDE.md는 *규약 SSOT 자체*이므로 1줄 예외 조항을 명시하여 처리 가능. 또는 표준 머리말 추가. Severity: **LOW** (작성 의도는 명확하나 grep 검사 false positive).

### A.2.2 DeepingSource repo 도메인 커버리지

**Repo 인벤토리 (변화)** `[VERIFIED:bash gh repo list DeepingSource --visibility public --limit 30 @2026-05-29]`:

| repo | 언어 | last_update | 추정 도메인 |
|---|---|---|---|
| `noisy-arl` | Python | 2026-04-29 | (description 공란) adversarial robustness research 추정 |
| `asan-med-img-obf-for-distribution` | Python | 2022-04-06 | (description 공란) 의료영상 비식별화 |

**총 public 2건** (직전 audit 0건 → +2). **H2 (DeepingSource public 0건) RESOLVED**.

**Role-dict §5 매핑 평가**:
- noisy-arl → `ml-researcher` (adversarial ML lens) + `security-researcher` (robustness 평가는 모델 보안 관련)
- asan-med-img-obf-for-distribution → `security-researcher` (privacy/de-identification) + `ml-engineer` (의료영상 처리)

**발견 사항 (1건)**:

- **N2 (신규, 정보). DeepingSource public 2건 모두 description 공란**: metadata 기반 도메인 추정은 *repo 이름 추론*에 의존. `[ASSUMPTION:repo 이름 기반 inference 검증방법: gh repo view <name> README 직접 확인 또는 owner confirm]`. 본 audit Step 2에서 mock 모드로 진행. Severity: **LOW** (정확도 한계 인지).
- **L2 (미해결, LOW 유지). role-dict §5 vision/CV lens 부재 가능성**: 본 audit run에서도 §5 전체 미확인. 차기 별도 audit run 권고 유지.

### A.2.3 built-prompt 내용의 흐름 (§1→§7) 문제

**평가 근거 인용**:
- G1 면제 불가 (CLAUDE.md System Invariant ②) [`CLAUDE.md:52`]
- Phase 5 회귀: P1-P5 fail → Phase 4 회귀 [`optimized-prompt-composer.md:264`]
- Phase 7 iteration cap 2회 [`optimized-prompt-composer.md:328`]
- `.specs/` gitignore: `[VERIFIED:bash git check-ignore -v .specs/test.composed.md @2026-05-29]` → `.gitignore:7:.specs/`

**흐름 점검 결과**:
- ⚠ **B2 (격상: M3→BLOCKING). G7 슬롯 부재 — 반복 발견**: composer §1 본문(`optimized-prompt-composer.md:64`)에서 "각 phase 사이에 명시적 gate (G1-G7)"라 선언했으나 G7 실 정의는 부재. eval-rubric §7 표는 "G6 = Phase 7 post-eval, lifecycle = §5 메타 평가"로 G7 슬롯 없음. 2026-05-26 A.2.3-F1과 동일. **§5.2 반복 발견 격상**. Severity: **BLOCKING(escalated)**.
- ⚠ **B3 (격상: M4→BLOCKING). Phase 6 실행 실패 분기 부재 — 반복 발견**: `optimized-prompt-composer.md` §7 본문 grep "실행 실패\|회귀.*Mode" 0 hit. Mode A 메인 세션 실행 실패, Mode B 사용자 paste 누락, Mode C API 호출 실패 분기 미명시. 2026-05-26 A.2.3-F2와 동일. **§5.2 반복 발견 격상**. Severity: **BLOCKING(escalated)**.
- ⚠ **B4 (격상: M5→BLOCKING). composed.md 회귀 이력 손실 — 반복 발견**: composer §5(`optimized-prompt-composer.md:242`) "최종 통과본으로 덮어쓰기" 정책 유지 + `.specs/` gitignore 상태. 회귀 이력 회복 수단 0. 2026-05-26 A.2.3-F3와 동일. **§5.2 반복 발견 격상**. Severity: **BLOCKING(escalated)**.

### A.2.4 built-prompt 내용 상충

**§3.4 자동 검사 결과** `[VERIFIED:bash @2026-05-29]`:
- rfc ↔ speckit 4-SSOT: 3 hit (speckit/CLAUDE.md/router) — **PASS** (composed prompt §3.4 grep 패턴은 정확히 3 hit 기대).
- `layer` 필드 echo: `grep -L "^| layer |" components/*.md` → 2 hit (`agent-role-dictionary`, `multi-agent-analysis-template`) — **부분 FAIL**.

**발견 사항 (3건)**:

- **B0 (격상: HIGH H1→BLOCKING). `layer` 필드 2/7 component 미echo — 반복 발견(부분 잔여)**: `agent-role-dictionary` / `multi-agent-analysis-template` 두 *분석 컨텐츠* layer component가 `## 0. Router 등록 metadata` section 자체를 보유하지 않음. 2026-05-26 H1과 동일 ID. 4 component는 commit `1643ad8`로 fix됨(`code-review-rubric`, `experiment-design-template`, `rfc-writing-template`, `autonomous-optimization-loop`) — **부분 fix이지만 잔여 → §5.2 반복 발견 격상**. Severity: **BLOCKING(escalated)**. 수정 권고: 두 component에 `## 0. Router 등록 metadata` section 신설 + `| layer | 분석 컨텐츠 |` 행 추가 (4 hit 패턴 follow).
- **B5 (격상: M2+M6→BLOCKING 묶음). `trigger.md` library 등록 0건 + A-2 DRY 위반 — 반복 발견**: CLAUDE.md/router/composer 어디에도 trigger.md 등록 없음 (CLAUDE.md:28 규약 *예시*로만 언급) + trigger-prompts.md A-2와 본문 중복 → silent drift 위험. 2026-05-26 A.2.4-F2 + A.2.1-F2와 동일. **§5.2 반복 발견 격상**. Severity: **BLOCKING(escalated)**.
- **M3-redux (감압: composer §7 Mode A ↔ Invariant ① 모호성)**: 2026-05-26 A.2.4-F3은 본 audit run에서 재검토 결과 *해석상 이슈*로 격하 가능. trigger-prompts.md v1.2가 "Mode A는 다음 conversation에서 메인 세션 시작" 의미로 명시 해석 가능 — composer §7 Mode A 단독으로는 모호하나 Invariant ①과 함께 읽으면 일관. **상태 유지: MEDIUM**, 격상 미적용. 단 composer §7 본문에 1줄 echo 권고 유지.

---

## A.3 3 test built-prompt 모의 실행 결과

> ⚠ 본 모의는 *Composer Phase 1→5 trace만* — 실제 Phase 6 dispatch 0건. spec.E echo, routing log, 발췌 ref는 작성하되 *실행 결과는 생성하지 않음* (System Invariant ① 준수).

### A.3.1 Test repo 선정 + 이유

`[VERIFIED:bash gh repo list DeepingSource --visibility public --limit 30 @2026-05-29]` 2 public + 1 OSS(routing 분기 다양성 cover):

| ID | repo | 언어 | 추정 도메인 | rough request |
|---|---|---|---|---|
| **T1** | DeepingSource `noisy-arl` | Python | adversarial robustness research (description 공란) | "noisy-arl의 robustness F1 metric을 trial budget 24회로 자동 탐색 개선" |
| **T2** | DeepingSource `asan-med-img-obf-for-distribution` | Python | 의료영상 비식별화 | "비식별화 알고리즘 3개(blur / inpainting / GAN) 비교 후 1개 채택 결정 (PSNR/SSIM + 위협 모델)" |
| **T3** | `vercel/next.js` (OSS, routing 보충) | TypeScript | web framework | "사내 Next.js v14 App Router 도입 RFC 작성 (vs Pages Router 유지)" |

**이질성 검증**: T1=Analysis+autonomous-optimization, T2=Decision+multi-agent+security, T3=Generation+rfc — 3개의 router 분기가 *모두 다름*. composer routing 다양성 cover.

`[ASSUMPTION:DeepingSource 2 public repo description 공란 — 도메인 추정은 repo 이름 기반. 검증방법: README webfetch 또는 owner confirm — 본 audit에선 metadata만 사용]`

### A.3.2 Mock composed prompt trace (Phase 1→5)

#### T1 — `noisy-arl-robustness-auto-tune`

**Phase 1 (Intake)** `[mock — .specs 파일 생성 0]`:
```
A. Purpose: noisy-arl의 adversarial robustness F1 metric을 자동 탐색으로 개선
B. Output: Analysis (단일 스칼라 metric 자동 반복 최적화)
C. Scale: 단일 metric (F1 또는 AUROC), trial budget 24회
D. Python env, REVERSIBLE (git keep-or-revert)
E. 성공: baseline 대비 metric ≥ X% 개선, trial 로그(results.tsv) + best commit + 학습 메모
G. Reversibility: [REVERSIBLE]
L. Domain: ml-researcher(1), security-researcher(2), systems-researcher(3)
```
✓ G1 통과.

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-29T mock] task=noisy-arl-robustness-auto-tune
- selected: [task-spec@1.0, autonomous-optimization-loop@1.0(§3+§4+§6), role-dict@1.x(ml-researcher×proposer; security-researcher×devils-advocate; systems-researcher×evidence-checker)]
- bypass: experiment-design(검증 단계 아님), multi-agent(대안 비교 아님), rfc/code-review/speckit(B 불일치)
- domain lens: [ml-researcher, security-researcher, systems-researcher]
- token estimate: ~18k / cap 50k (실비용은 trial × measure dominate)
- multi-LLM: off
- rationale: 단일 스칼라 자동 측정 + REVERSIBLE → autonomous-optimization-loop 명확 분기
```
✓ G2 통과 (cap 4 이내).

**Phase 3-5 (요약)**:
- §4 Activated: autonomous-optimization §3 Step 0-5 + §4 Quality Gates + §6 anti-patterns 발췌.
- §6 Output Gates: results.tsv 형식 + best commit hash + 학습 메모(memory durable pattern) + baseline 대비 % 명시.
- P1-P5 PASS. ⚠ smell 2.4 — autonomous-optimization §3 Step 0 "measure 실패 시 revert"는 명시되어 있으나 composer §5 발췌 가이드에 *fallback 절도 발췌* boilerplate가 없어 사용자가 발췌 시 누락 가능.

#### T2 — `asan-med-img-obf-method-decision`

**Phase 1 (Intake)** `[mock]`:
```
A. Purpose: 의료영상 비식별화 알고리즘 3개 중 1개 채택 결정 (blur / inpainting / GAN-based)
B. Output: Decision
C. Scale: 3 topic
D. 한국어 markdown, security/privacy critical, PII redaction 필수
E. 성공: (a) 각 방식 위협 모델 명시 (b) PSNR/SSIM 비교 (c) HIPAA/PIPA-style 검증 명시 (d) 1주 PoC 가능 actionability
G. Reversibility: [COSTLY-TO-REVERSE] (의료 데이터 재배포 결정이므로)
L. Domain: security-researcher(1), ml-engineer(2), data-engineer(3)
```
✓ G1 통과.

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-29T mock] task=asan-med-img-obf-method-decision
- selected: [task-spec@1.0, multi-agent-template@1.x(§2+§4+§7+§8), role-dict@1.x(security-researcher×devils-advocate; ml-engineer×proposer; data-engineer×evidence-checker)]
- bypass: autonomous-optimization(대안 비교, 단일 탐색 아님), rfc/speckit(B=Generation 아님), code-review(B=Review 아님)
- domain lens: [security-researcher, ml-engineer, data-engineer]
- token estimate: ~26k / cap 50k
- multi-LLM: COSTLY-TO-REVERSE → 검토 후보 (실제 활성은 spec.K 미명시로 default off)
- rationale: 3 topic + Decision + privacy/security critical + COSTLY-TO-REVERSE → multi-agent 동일 criteria 평가 + 보안 lens 비판 강도 필요
```
✓ G2 통과.

**Phase 3-5 (요약)**:
- §4 Activated: multi-agent §2 framework + §4 workflow + §7 gates + §8 anti-patterns 발췌. role-dict §5 security-researcher / ml-engineer 발췌.
- §6 Output Gates: spec.E echo + 위협 모델 표 + PSNR/SSIM 측정 결과 + 1주 PoC 액션.
- P1-P5 PASS. ⚠ smell 2.4 — multi-agent §3.1 C-016 "파일을 읽지 못하면 즉시 abort"는 명시되어 있으나 발췌 가이드에 fallback 절 횡단 강제력 없음 (T1과 동일).
- ⚠ P5 redaction: 환자 식별자 / 병원 이름 등 PII는 의료영상 비식별화 prompt 자체에 *예제로 포함되면* leak 위험 → 본 mock에서는 placeholder 처리.

#### T3 — `next-js-v14-app-router-rfc`

**Phase 1 (Intake)** `[mock]`:
```
A. Purpose: 사내 Next.js v14 App Router 도입 RFC 작성 (vs Pages Router 유지)
B. Output: Generation
C. Scale: 3-5 페이지 한국어 markdown RFC
D. 한국어 + 영문 기술 용어 병기, team 합의 필요
E. 성공: (a) motivation (b) alternatives (Pages Router 유지/App Router 신규/Hybrid) (c) tradeoffs (d) rollout plan (e) reviewer feedback round 1회 통과
G. Reversibility: [REVERSIBLE] (RFC 문서이므로)
F. Audience: 사내 frontend team
L. Domain: frontend-engineer(1), pedagogy-reviewer(2)
```
✓ G1 통과.

**Phase 2 (Routing)**:
```
[ROUTING 2026-05-29T mock] task=next-js-v14-app-router-rfc
- selected: [task-spec@1.0, rfc-writing-template@1.0(§3+§4), role-dict@1.x(pedagogy-reviewer×proposer; consistency-checker)]
- bypass: speckit-spec-generation (키워드 미일치 — spec.A에 spec/plan/tasks/SDD 없음 + 자유 형식 RFC), multi-agent (단일 RFC, multi-topic 아님), code-review/experiment/autonomous (B 불일치)
- domain lens: [frontend-engineer, pedagogy-reviewer]
- token estimate: ~16k / cap 50k
- multi-LLM: off
- rationale: Generation + 자유 형식 + team 합의 → rfc-writing-template. speckit과 *상호 배타* — router §2 키워드 우선 분기로 rfc 채택. (✓ 4-SSOT PASS)
```
✓ G2 통과. **rfc ↔ speckit 상호 배타 룰이 작동함** (B5/B0 잔여에도 불구하고 routing 자체는 영향 없음).

**Phase 3-5 (요약)**:
- §4 Activated: rfc §3 Step 0-6 + §4 Quality Gates 발췌.
- §6 Output Gates: spec.E echo + reviewer feedback log + tradeoff matrix + rollout plan.
- P1-P5 PASS. ⚠ smell 2.4 — rfc §3 "reviewer round skip 정책"이 component-local로 명시되어 있으나 composer §5 발췌 가이드에 fallback boilerplate 부재.

### A.3.3 평가 매트릭스 — prompt-evaluation-rubric §1 P1-P7 + §2 smell

> 3 mock built-prompt 자체에 대한 평가. 본 audit auditor가 작성·평가 → `[self-eval 경고]` 부착.

| Test | P1 | P2 | P3 | P4 | P5 | P6 | P7 | s2.1 Redund | s2.2 Contra | s2.3 Vague gate | s2.4 Missing fallback | s2.5 Anchoring | s2.6 Over-scoping |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **T1** | ✓ | ✓ | ✓ | ✓ | n/a (no PII) | ⚠ ~18k OK / 실비용 trial dom | ✓ 단일 metric 자동 탐색 — composition 가치 명확 | ✓ | ✓ | ✓ G0-G5 명시 | **⚠ measure 실패 fallback 발췌 누락 위험 (composer §5 boilerplate 부재)** | ✓ | ✓ |
| **T2** | ✓ | ✓ | ✓ | ✓ | ⚠ PII (환자 ID) redact 필수 (mock placeholder) | ✓ ~26k | ✓ 3 topic+critical | ⚠ multi-agent §7 gates ↔ rubric P3 일부 중복 | ✓ | ✓ | **⚠ baseline 읽기 실패 fallback 명시 필요 (multi-agent §3.1 C-016 발췌 누락 가능)** | ✓ | ✓ |
| **T3** | ✓ | ✓ | ✓ | ✓ | ✓ (사내 식별자 마스킹) | ✓ ~16k | ✓ external+합의 → rfc 가치 | ✓ | ✓ | ✓ rfc §4 quality gates | **⚠ reviewer 응답 missing 시 fallback (rfc §3 skip 정책 발췌 누락 가능)** | ✓ | ✓ |

**[self-eval 경고]** — 본 매트릭스는 auditor가 작성·자가 채점. 신뢰도 보장 위해 별도 LLM(gemini-bridge / codex-bridge) 또는 별도 Claude 세션에서 blind A/B 재채점 권고. self_upgrade §B4 정책 준수.

### A.3.4 Synthesis — 3 test 공통 약점 (반복 발견)

multi-agent-analysis-template §4 synthesis 단계 모의 적용:

> 3 mock built-prompt 모두에서 `smell 2.4 Missing fallback`이 ⚠ 단계로 검출됨 — **2026-05-26 audit A.3.4와 100% 동일한 발견**. T1은 measure 실패, T2는 baseline 읽기 실패, T3는 reviewer 응답 missing 시 fallback이 component-local로 명시되어 있으나 **composer §5 Phase 4 발췌 가이드(`optimized-prompt-composer.md:227-235`)에 "fallback 절도 발췌" 횡단 boilerplate가 부재**. 본 audit에서 grep 검증 — `grep "fallback\|실패 시" optimized-prompt-composer.md` 발췌 가이드 section에서 hit 0. 2026-05-26 M7과 동일 ID → **§5.2 반복 발견 격상: BLOCKING**. 수정 권고: composer §5 "발췌 가이드" 또는 "Component 발췌 룰"에 "각 component에서 *failure-mode fallback 절*도 함께 발췌" 1줄 추가 (M7 fix와 동일).

---

## A.4 발견 사항 priority 표

### BLOCKING (8건 — 모두 §5.2 반복 발견 격상)

| ID | 이전 ID | 발견 | 재현 절차 | 수정 권고 | 파일 |
|---|---|---|---|---|---|
| **B0** | H1 | `layer` 필드 2/7 component 미echo (`agent-role-dictionary`, `multi-agent-analysis-template`) — `## 0. Router 등록 metadata` section 자체 없음 | `grep -L "^\| layer \|" prompts/prompt-composer-system/components/*.md` → 2 hit | 두 component에 `## 0. Router 등록 metadata` section 신설 + `\| layer \| 분석 컨텐츠 \|` 행 추가. router §1 표에도 동시 반영. | `components/agent-role-dictionary.md`, `components/multi-agent-analysis-template.md` |
| **B1** | M1 | `trigger.md` 표준 머리말 위반 | `head -3 prompts/prompt-composer-system/trigger.md` → 머리말 없음 | 표준 3필드 머리말 추가 + "아래 구분선부터가 복사 본문" 명시 (CLAUDE.md:28 규약 echo) | `prompts/prompt-composer-system/trigger.md` |
| **B2** | M3 | G7 슬롯 부재 / G6 의미 중복 | `optimized-prompt-composer.md:64` "G1-G7" 선언 ↔ eval-rubric §7 표 = G6 종착 | 옵션 A: G1-G6로 통일 + lifecycle 메타 평가는 별도 gate 없음 명시. 옵션 B: G7을 Phase 7 종료, G6를 Phase 5 swap. | composer §1/§6/§8 + eval-rubric §7 |
| **B3** | M4 | Phase 6 실행 실패 분기 부재 | `grep "실행 실패\|회귀.*Mode" optimized-prompt-composer.md` → 0 hit | composer §7 끝에 "실행 실패 시 Mode A는 Phase 4 회귀, Mode B는 사용자 paste 누락 보고, Mode C는 API 오류 분기" 한 단락 추가 | composer §7 |
| **B4** | M5 | composed.md 회귀 시 덮어쓰기 — 회귀 이력 손실 (`.specs/` gitignore 처리됨) | `cat .gitignore \| grep .specs` ✓ + `optimized-prompt-composer.md:242` "덮어쓰기" | 옵션 A: `.specs/<task-id>.composed.iter-<N>.md` 버전 suffix. 옵션 B: composer log에 prompt diff 첨부. 옵션 C: 현 상태 유지 + 위험 명시(권고 약함) | composer §5 산출물 저장 |
| **B5** | M2+M6 | `trigger.md` library 등록 0건 + A-2 DRY 위반 (silent drift 위험) | `grep "trigger.md" CLAUDE.md optimized-prompt-composer.md prompt-component-router.md` → CLAUDE.md:28(규약 예시)만 hit, 등록 0건 | trigger.md에 "trigger-prompts.md A-2의 paste-ready 사본임" 1줄 명시 + CLAUDE.md "인터페이스" 절에 trigger.md 등록 1줄. B1과 동일 PR로 묶을 수 있음. | trigger.md / CLAUDE.md |
| **B6** | M7 | composer §5 Phase 4 발췌 가이드에 *failure-mode fallback* 횡단 boilerplate 부재 | 3 mock test (T1/T2/T3) 모두에서 smell 2.4 ⚠ 재현 | composer §5 "발췌 가이드"에 "각 component에서 fallback 절도 함께 발췌" 1줄 추가 | composer §5 |
| **B7** | L1 | G4 spec.E echo grep 단위 게이트 부재 | `optimized-prompt-composer.md:247-252` G4 체크리스트에 "spec.E 각 criterion이 §6에 echo" 없음 | G4 체크리스트에 "spec.E의 각 criterion이 §6 Output Gates에 grep 단위로 echo됨" 항목 추가 | composer §5 Gate G4 |

### MEDIUM (1건 — 격상 미적용)

| ID | 이전 ID | 발견 | 비고 |
|---|---|---|---|
| M3-redux | M3(A.2.4-F3) | composer §7 Mode A ↔ Invariant ① 모호성 | 본 audit run에서 해석상 이슈로 격하 가능 — trigger-prompts.md v1.2 invariant와 함께 읽으면 일관. composer §7 1줄 echo 권고 유지하나 격상 미적용. |

### LOW (4건)

| ID | 이전 ID | 발견 | 비고 |
|---|---|---|---|
| L2 | L2 | role-dict §5 vision/CV lens 부재 가능 | 미해결, 별도 audit run 권고 유지 (본 run에서도 §5 전체 미read) |
| L3 | L3 | composer §0 frontmatter default 모드 식별자 부재 | UNRESOLVED. §5.2 적용 시 격상 후보지만 정보성 finding이라 LOW 유지 권고 |
| L4 | A.4-F1 | README.md에 prompt-composer-system 절 없음 | UNRESOLVED. 외부 사용자 onboarding 영향, 정책 결정 필요 |
| **N1** | (신규) | CLAUDE.md 자기 규약 위반 — 표준 머리말 0 | 자기-참조 모순. 1줄 예외 조항 명시 또는 표준 머리말 추가 |
| **N2** | (신규, 정보) | DeepingSource public 2건 모두 description 공란 | metadata-only inference 정확도 한계 (Step 2 mock 모드 합당) |

### Δ from 2026-05-26 요약

| 분류 | 2026-05-26 | 2026-05-29 | 변화 |
|---|---|---|---|
| BLOCKING | 0 | **8** | **+8 (반복 발견 격상)** |
| HIGH | 2 (H1, H2) | 0 | -2 (H1→B0 격상, H2 RESOLVED) |
| MEDIUM | 7 (M1-M7) | 1 | -6 (M1→B1, M2+M6→B5, M3→B2, M4→B3, M5→B4, M7→B6 격상; M3-redux 격하 유지) |
| LOW | 3 (L1-L3) | 5 (L2, L3, L4, N1, N2) | +2 (L1→B7 격상, L4 신규로 분리, N1+N2 신규) |
| RESOLVED | — | **H2** | +1 RESOLVED |

---

## A.5 self_upgrade "1회 1대상" 단위 PR 제안

> `self_upgrade.md §0` "1회 실행 = 대상 프롬프트 1개" + "작은 diff" + "사람 승인 게이트" 준수. 본 audit run에선 직접 mutation 0건 — 모든 PR은 사용자 승인 후 별도 conversation에서.

### PR 1 — `feat(prompts): add Router metadata section + layer field to 2 분석 components` [BLOCKING B0]
- 변경: `agent-role-dictionary.md`, `multi-agent-analysis-template.md`에 `## 0. Router 등록 metadata` section 신설 + `| layer | 분석 컨텐츠 |` 행 추가. router §1 표에도 동시 반영.
- diff 추정: 2 component × ~12줄 (section 신설) + router §1 표 2행 = ~30줄.
- 검증: `grep -L "^| layer |" prompts/prompt-composer-system/components/*.md` → 0 hit.
- 우선순위: **최우선** (반복 발견 BLOCKING, System Invariant ④ 강제력 핵심).

### PR 2 — `fix(prompts/trigger.md): add standard header + decouple from A-2 drift + register in CLAUDE.md` [BLOCKING B1+B5]
- 변경 (소): trigger.md 머리말 추가 + "trigger-prompts.md A-2의 paste-ready 사본 — 정본 수정 시 동기화 필요" 1줄 명시 + CLAUDE.md "인터페이스" 절에 trigger.md 1줄 등록.
- diff 추정: 2 파일 × ~8줄 = ~16줄.
- 또는 변경 (대): trigger.md 삭제 + 사용자 README A-2 paste 안내 → 별도 PR로 격상.

### PR 3 — `docs(composer): unify G1-G6 + remove G7 phantom` [BLOCKING B2]
- 변경: composer §1, §6 표, eval-rubric §7에서 G7 표현을 G6로 통일. lifecycle 메타 평가는 "(분기 cycle)" 슬롯 별도 명시.
- diff 추정: 3 파일 × 1-3줄.
- 검증: `grep "G7\|G1-G7" prompts/prompt-composer-system/*.md` → 0.

### PR 4 — `docs(composer): add Phase 6 execution failure routing` [BLOCKING B3]
- 변경: composer §7 끝에 mode-별 실패 회귀 1단락 추가 (Mode A/B/C 각각).
- diff 추정: 1 파일 × ~10줄.

### PR 5 — `docs(composer): preserve regression history of composed.md` [BLOCKING B4]
- 변경: composer §5 산출물 저장 규약에 회귀 버전 suffix (`<task-id>.composed.iter-<N>.md`) 또는 log 첨부 정책 명시. 옵션 결정은 사용자 선택.
- diff 추정: 1 파일 × ~5줄.

### PR 6 — `docs(composer): require failure-mode fallback in component excerpts` [BLOCKING B6]
- 변경: composer §5 "발췌 가이드" 또는 "Component 발췌 룰"에 "각 component에서 fallback 절도 함께 발췌" 1줄 추가.
- diff 추정: 1 파일 × 1줄.

### PR 7 — `docs(composer): G4 require spec.E echo grep` [BLOCKING B7]
- 변경: composer §5 Gate G4 체크리스트에 "spec.E의 각 criterion이 §6 Output Gates에 grep 단위로 echo됨" 항목 추가.
- diff 추정: 1 파일 × 1줄.

### LOW PR (배치 가능)
- PR 8: N1 — CLAUDE.md 1줄 예외 조항 (예: "단, 본 CLAUDE.md는 디렉토리 규약 자체이므로 § 0 헤더 형식 예외") 또는 표준 머리말 추가.
- PR 9: L3 — composer §0 frontmatter description에 "default: A-2 compose-only auto" 1줄 echo.
- PR 10: L4 — README.md에 prompt-composer-system 1단락 추가 (별도 PR).
- PR 11: L2 — role-dict §5 vision lens 별도 audit run으로 진행.

**총 7 BLOCKING PR + 4 LOW PR**. 각각 self_upgrade §0 1회 1대상 + 작은 diff. **§B4 블라인드 채점 권장**: PR 1, 3, 4, 5, 6 (composer 본체 변경).

### 자동화 권고 (반복 발견 패턴 차단)

8건 BLOCKING 중 7건이 2026-05-26에서 변경 없이 누적됨. 자동화 권고:
1. **CI 가드**: `grep -L "^\| layer \|" components/*.md` (B0), `grep -c "G1-G[67]" *.md` (B2) 등을 pre-commit/CI에 추가하여 prompt-composer-system 갱신 시 즉시 차단.
2. **audit 주기 정례화**: 본 audit prompt를 `/loop` 또는 `/schedule`로 정기 실행 (composer Phase 외, 진단 도구 layer).
3. **BLOCKING backlog 정책**: 반복 BLOCKING 발견은 자동 PR 생성 (GitHub Actions + claude-code) 검토.

---

## A.6 출처 (tier별)

### Tier 1 (1차 SSOT — repo 내부 정본)
- `prompts/prompt-composer-system/CLAUDE.md` `[VERIFIED:Read @2026-05-29]`
- `prompts/prompt-composer-system/audit-composer-system.md` (본 audit prompt) `[VERIFIED:Read @2026-05-29]`
- `prompts/prompt-composer-system/optimized-prompt-composer.md` (Phase 1-7 orchestrator) `[VERIFIED:Read @2026-05-29]`
- `prompts/prompt-composer-system/prompt-component-router.md` `[VERIFIED:Read head @2026-05-29]`
- `prompts/prompt-composer-system/prompt-evaluation-rubric.md` `[VERIFIED:Read @2026-05-29]`
- `prompts/prompt-composer-system/trigger.md`, `trigger-prompts.md` (head only) `[VERIFIED:Read head @2026-05-29]`
- `prompts/prompt-composer-system/task-spec-template.md`, `context-injection-patterns.md` `[VERIFIED:Read head @2026-05-29]`
- `prompts/prompt-composer-system/component-discovery-{collect,approve,ledger}.md` `[VERIFIED:Read head @2026-05-29]`
- `prompts/prompt-composer-system/components/*.md` 7파일 `[VERIFIED:Read head @2026-05-29]` + `agent-role-dictionary`, `multi-agent-analysis-template` §0 잔여 metadata 확인 `[VERIFIED:sed @2026-05-29]`
- `self_upgrade.md`, `self_upgrade-changelog.md`, `README.md` `[VERIFIED:Read head @2026-05-29]`
- `.gitignore` (`.specs/`) `[VERIFIED:bash git check-ignore @2026-05-29]`

### Tier 1.5 (이전 audit 보고서 — 반복 발견 격상 근거)
- `docs/projects/composer-system-audit/2026-05-26.md` `[VERIFIED:Read @2026-05-29]`

### Tier 2 (외부 — gh CLI)
- `[VERIFIED:bash gh repo list DeepingSource --visibility public --limit 30 @2026-05-29]` public repo 2건 (noisy-arl, asan-med-img-obf-for-distribution).

### Tier 3 (추정 — 검증 미수행)
- `[ASSUMPTION:DeepingSource 2 public repo 도메인 추정은 repo 이름 기반 — 검증방법: 각 repo README webfetch 또는 owner confirm]`
- `[ASSUMPTION:role-dict §5에 vision/CV lens 부재 가능 — 검증방법: agent-role-dictionary.md §5 전체 read (본 run 미수행)]`
- `[ASSUMPTION:vercel/next.js 분기 cover용 OSS 선정은 routing 다양성 보충 목적 — 사내 도입 실제 검토 의도 아님]`

---

## A.7 위험 / 보류 사항

- **[self-eval 경고]** 본 audit의 모든 점수·매트릭스·priority는 메인 세션 자기 진단. self_upgrade §B4 정책에 따라 *블라인드 독립 채점* 권고 — 별도 LLM(`codex-bridge` / `gemini-bridge`) 또는 별도 Claude 세션에 본 보고서 + audit prompt를 블라인드 입력으로 전달하여 5축 재채점.
- **8 BLOCKING 격상은 §5.2 룰 적용 결과** — backlog 관리 신호로 해석 가능. 사용자가 의도적으로 deferred한 항목이라면 audit prompt §5.2 룰을 *해제 조건*(예: "한 번 격상 후 사용자 dismiss 처리 시 차기 audit에서 LOW로 재기록")으로 보강 검토 권고.
- **DeepingSource public 2건 모두 description 공란** — 도메인 추정 정확도 한계. 본 audit Step 2 T1/T2의 routing 분기는 *repo 이름 기반*. 정확도 보강은 owner confirm 또는 README webfetch 필요.
- **본 audit run에서 mutation 0건** — 본 보고서 Write만 read-only 산출로 분류 (§7 audit log 누적 예외). PR 1-11은 **Step 5 사용자 승인 후만** 별도 conversation에서 진행.
- **`agent-role-dictionary.md` / `multi-agent-analysis-template.md`에 `## 0. Router 등록 metadata` section 신설**은 단순 1행 추가가 아니라 *분석 컨텐츠 layer가 router 등록 의무를 어떻게 따를지*에 대한 합의 필요 — `bypass-vs-routable` 정책 결정 후 PR 진행 권고. 본 audit run에선 기본값 가정 (`component name` + `layer` 2행 minimal).

---

## A.8 메타 — 본 audit의 한계

1. **components/multi-agent-analysis-template.md 부분 read만** (직전 run과 동일). 61KB 중 §0-§1 (~100줄)만 read. §2-§8 전체는 미확인.
2. **agent-role-dictionary.md 부분 read만** (직전 run과 동일). 33KB 중 §0-§1 (~120줄)만 read.
3. **component-discovery-{collect,approve}.md head만 read**.
4. **본 audit이 *자기 자신*도 점검 대상**(§3.5 5항목 자동 검증) — 5/5 PASS, but 자기-진단이므로 self-eval bias. blind external review 권고.
5. **L2 vision/CV lens 추가 audit 미진행** (직전 run에서도 deferred). 차기 audit run에서 분리 진행.

---

*보고서 끝. 본 audit run에선 mutation 0건. PR 제안 1-11은 Step 5 사용자 승인 후 별도 conversation에서 진행.*
