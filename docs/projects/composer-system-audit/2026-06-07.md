# Composer System Audit — 2026-06-07

> **무엇**: `prompts/prompt-composer-system/` 자체를 정기 진단하는 audit 보고서 (3회차). composer Phase 1-7 (G1-G7)을 거치지 않고, audit prompt 자체를 별도 layer(거버넌스·메타 진단 도구 — 4번째 축)에서 실행.
> **용도**: 시스템 변경 후 정기 점검. 직전 audit(2026-05-29) 대비 Δ + 반복 발견 BLOCKING 유지/격상. 이번 run 특이점: `builder/` 리팩터(`f86cc2a`) + code-review-rubric v1.1(`7f800be`) 반영 후 첫 audit.
> **시리즈 위치**: 독립 실행 — composer Phase 밖, build 흐름에 호출 금지. (System Invariant ① 준수)
>
> Audit 입력: `prompts/prompt-composer-system/audit-composer-system.md` (Conv2 직접 실행 — audit prompt 정형이므로 합법)
> Audit 실행: Conv2 (본 conversation, 2026-06-07) — Phase 6 실 dispatch 0, 모의 trace만.
> Audit 평가: Conv3 권고 (별도 LLM 또는 별도 세션 블라인드 채점) — `[self-eval 경고]` 부착.

---

## 0. Audit run 메타

- Auditor: claude-opus-4-8 (1M context), Claude Code 메인 세션 (`/Users/yeonhuigim/workspace/vh1981_skills`)
- Branch: `main`
- Working tree: prompt-composer-system/ clean (직전 세션 commit `7f800be` 반영 완료). audit run은 read-only + 본 보고서 Write만.
- `[VERIFIED:bash gh repo list DeepingSource --visibility public @2026-06-07]` public repo **2건** (noisy-arl, asan-med-img-obf-for-distribution) — 2026-05-29와 동일.
- `[self-eval 경고]` 본 audit은 메인 세션 자기 진단 → self-eval bias 위험. 블라인드 독립 채점은 §A.7 권고.
- **반복 발견 처리**: §5.2 룰 — 동일 발견 ID가 이전 보고서(2026-05-26 / 2026-05-29)에 명시 + 현 run에서 PASS 못 받으면 BLOCKING 유지/격상.
- **이번 run 신규 컨텍스트**: ⚠ 직전 audit 이후 `builder/` 서브디렉토리 리팩터(`f86cc2a`)가 발생 → audit 자체의 일부 기계 검사 command가 stale(N3). 또한 직전 audit auditor가 본 세션에서 code-review-rubric v1.1을 빌드(`7f800be`)했으므로 그 결과물도 점검 대상이나 **self-build self-audit** 편향 주의(§A.8).

---

## A.1 시스템 개관 — 인벤토리 + Δ from 2026-05-29

### A.1.1 디렉토리 구조 변화 (★ 이번 run 핵심)

`f86cc2a` 리팩터로 조합 인프라 5파일이 `builder/`로 이동:

| 영역 | 파일 수 | Δ |
|---|---|---|
| `prompt-composer-system/` (root) | 7 (audit, CLAUDE, trigger, trigger-prompts, discovery×3) | 조합 인프라 5개 → builder/로 빠짐 |
| `prompt-composer-system/builder/` (신규) | 5 (optimized-prompt-composer, task-spec-template, prompt-component-router, context-injection-patterns, prompt-evaluation-rubric) | **신규 디렉토리** |
| `prompt-composer-system/components/` | 7 | 변경 없음 |
| **합계** | **19** | (직전 audit 표기 19와 동일하나 *위치* 이동) |

`[VERIFIED:bash ls @2026-06-07]` root 7 + builder 5 + components 7 = 19.

### A.1.2 components/ `layer` 필드 echo — B0 RESOLVED ✓

`[VERIFIED:bash grep -L "^| layer |" components/*.md → 0 hit @2026-06-07]`

| component | layer | `| layer |` echo | Δ |
|---|---|---|---|
| agent-role-dictionary | 분석 컨텐츠 | ✓ | **commit `d761a0f` — B0 fix** |
| multi-agent-analysis-template | 분석 컨텐츠 | ✓ | **commit `d761a0f` — B0 fix** |
| code-review-rubric | domain content | ✓ | **commit `7f800be` v1.0→1.1 (§9/§10)** |
| experiment-design-template | domain content | ✓ | 변경 없음 |
| rfc-writing-template | domain content | ✓ | 변경 없음 |
| autonomous-optimization-loop | domain content | ✓ | 변경 없음 |
| speckit-spec-generation | domain content | ✓ | 변경 없음 |

**B0 (H1) RESOLVED** — 7/7 component 모두 `layer` 필드 보유. `## 0. Router 등록 metadata` section이 2개 분석 컨텐츠 component에 신설됨(`d761a0f`).

### A.1.3 code-review-rubric v1.1 무결성 (N5 — 정보/positive)

`[VERIFIED:bash @2026-06-07]` §9(Custom rule layer) 1 + §10(대형 changeset) 1 + `| version | 1.1 |` 1 + layer 필드 유지 + 머리말 무결.
- rfc↔speckit 상호배타 4-SSOT: 3 hit 유지 (영향 없음).
- 회귀 없음 — v1.1 추가가 기존 8축/차단조건/router 매핑을 깨지 않음. **N5 = 정상**.

---

## A.2 Checklist 4항목 평가

### A.2.1 손상 위험 (§3.1)

`[VERIFIED:bash header check @2026-06-07]` MISSING_HEADER 2건: `CLAUDE.md`, `trigger.md`.

- **B1 (반복 3회차 — BLOCKING 유지). `trigger.md` 표준 머리말 위반**: `head -3 trigger.md` 첫 줄 `너는 지금부터 optimized-prompt-composer로 동작한다.` — 머리말 0. CLAUDE.md "표준 머리말 형식" 규약(trigger.md를 *예시로 명시*)을 2026-05-26/05-29/06-07 **3회 연속 미준수**. Severity: **BLOCKING(recurring×3)**.
- **N1 (반복 2회차 — LOW 유지). `CLAUDE.md` 자기 규약 위반**: CLAUDE.md가 "모든 *.md는 표준 머리말로 시작" 선언하면서 자기 자신은 미보유. 규약 SSOT이므로 1줄 예외 조항으로 처리 가능. Severity: **LOW**.
- ★ **N3-a (신규 — HIGH). §3.1 header-check 루프가 `builder/` 미포함**: `for f in prompts/prompt-composer-system/*.md ... components/*.md` 루프가 `builder/*.md` 5파일을 **검사 대상에서 누락**. 즉 조합 인프라 5개(composer/router/rubric/task-spec/context-injection)의 머리말은 audit이 *자동 검증하지 않음*. 수동 확인 결과 5개 모두 머리말 보유(현재는 무해)이나, **audit의 핵심 검사 sample이 리팩터 후 5/19 파일을 놓치는 구조적 drift**. 원인: `f86cc2a` 리팩터가 §3.1 루프에 미반영. Severity: **HIGH (audit 자체 무결성)**.

### A.2.2 도메인 커버리지 (§3.2)

`[VERIFIED:bash gh repo list DeepingSource --visibility public @2026-06-07]`:

| repo | 언어 | last_update | 추정 도메인 |
|---|---|---|---|
| `noisy-arl` | Python | 2026-04-29 | (description 공란) adversarial/robust ML 추정 |
| `asan-med-img-obf-for-distribution` | Python | 2022-04-06 | (description 공란) 의료영상 비식별화 (CV + privacy) |

- **L2 (반복 — 격상: ASSUMPTION→VERIFIED, severity LOW→LOW-MEDIUM). role-dict §5 vision/CV lens 부재 확정**: 본 run에서 `agent-role-dictionary.md §5` 전체 read 완료 — §5.1~§5.11 (backend/frontend/mobile/devops-sre/embedded/data-engineer/ml-engineer/ml-researcher/security-researcher/systems-researcher/hci-researcher). **vision/CV 전용 lens 없음 확정** `[VERIFIED:Read agent-role-dictionary.md §5 @2026-06-07]`. DeepingSource `asan-med-img-obf`(의료영상=CV) + 회사 도메인(de-identification/CV/privacy)을 고려하면 *실재 도메인 gap*. 직전 audit까지 ASSUMPTION이었으나 본 run에서 §5 전체 read로 VERIFIED 격상. Severity: **LOW-MEDIUM** (breakage 아님, lens 추가 enhancement — component-discovery collect 후보 적격).
- **N2 (반복, 정보). DeepingSource public 2건 description 공란** — metadata-only inference. `[ASSUMPTION:repo 이름 기반 — README webfetch/owner confirm로 검증]`.

### A.2.3 흐름 (§1→§7) (§3.3)

`[VERIFIED:bash @2026-06-07]`:
- `.specs/` gitignore: `git check-ignore -v .specs/test.composed.md` → `.gitignore:7:.specs/` (유지).

- **B2 (반복 — BLOCKING 유지). G7 phantom**: `builder/optimized-prompt-composer.md:68` "각 phase 사이에 명시적 gate (G1-G7)" ↔ `builder/prompt-evaluation-rubric.md §7` 표 = G6 종착(Phase 5→G5, Phase 7→G6, lifecycle→분기 cycle). G7 실 정의 부재. `[VERIFIED:bash grep G1-G7 → composer:68 1 hit / rubric 0 hit]`. Severity: **BLOCKING(recurring)**.
- **B3 (반복 — BLOCKING 유지). Phase 6 실행 실패 분기 부재**: `grep "실행 실패\|회귀.*Mode" composer` → 0 hit. Mode A/B/C 실패 회귀 미명시. Severity: **BLOCKING(recurring)**.
- **B4 (반복 — BLOCKING 유지). composed.md 회귀 이력 손실**: `composer:246` "최종 통과본으로 덮어쓰기" + `.specs/` gitignore → 회귀 이력 복구 수단 0. `iter-` 버전 suffix 없음. Severity: **BLOCKING(recurring)**.

### A.2.4 상충 (§3.4)

`[VERIFIED:bash @2026-06-07]`:
- rfc↔speckit 4-SSOT: 3 hit (speckit/CLAUDE.md/router) — **PASS**.
- `layer` echo: 0 미echo — **PASS** (B0 resolved).

- **B5 (반복 — BLOCKING 유지, 양상 변화). `trigger.md` DRY drift + 등록**: `f86cc2a`로 `build_prompt.md` 커맨드가 `@prompts/prompt-composer-system/trigger.md`를 wrap → trigger.md가 이제 **command의 의존 대상**이 됨(고아 risk 일부 완화). 그러나 ① trigger.md 자체에 "trigger-prompts.md A-2의 paste-ready 사본" DRY 동기화 note **여전히 0** (`grep "A-2\|사본" trigger.md` → 0) ② CLAUDE.md의 trigger.md 언급 1건은 *규약 예시*(머리말 형식)일 뿐 인터페이스 *등록*이 아님. → trigger.md / trigger-prompts.md A-2 / build_prompt.md 3곳이 silent drift 가능. **소비자가 2개(command+수동)로 늘어 DRY 중요도 오히려 상승**. Severity: **BLOCKING(recurring)**.

### A.2.5 자기 점검 (§3.5 — S1-S5)

`[VERIFIED:bash @2026-06-07]`:

| # | 항목 | 결과 | 판정 |
|---|---|---|---|
| S1 | `## 0. Router 등록 metadata` 미보유 | 0 | ✓ PASS |
| S2 | router/composer 등록 0건 | 0 hit | ✓ PASS |
| S3 | 머리말 ⚠️ ≥2 | 2 | ✓ PASS |
| S4 | self-eval 경고 ≥2 | 6 | ✓ PASS |
| S5 | Step 0-5 + Gate 표 ≥10 | **0** | ✗ **FAIL (false negative)** |

- ★ **N4 (신규 — MEDIUM). S5 grep command 오작동(false FAIL)**: §3.5 S5 command `grep -cE "^- \*\*Step [0-5]\|^\| Gate \|^\| \*\*G[0-5]\*\*"`가 ERE에서 `\|`를 *literal pipe*로 해석 → 단일 패턴이 되어 0 hit. **수정 패턴**(`grep -cE "^- \*\*Step [0-5]|^\| \*\*G[0-5]\*\*|^\| Gate "`) 적용 시 **13 hit (Step 6 + Gate 6 + 헤더 1)** → intent PASS. 즉 audit 구조는 정상이나 *self-check command 자체가 버그*라 G3에서 **거짓 회귀를 유발**. Severity: **MEDIUM (audit 자체 무결성)**.
- **판정**: S5 *intent* PASS(13), *command* FAIL → G3는 "intent PASS, command 버그(N4)"로 기록. 거짓 FAIL로 audit을 중단하지 않음.

---

## A.3 3 test built-prompt 모의 실행 (Phase 1→5 trace, 실 dispatch 0)

### A.3.1 Test repo 선정 (이질성 + v1.1 검증 목적)

`[VERIFIED:bash gh repo list DeepingSource @2026-06-07]` 2 public + 1 OSS 보충. 이번 run은 **T2를 Review 분기**로 설정해 직전 세션 빌드한 code-review-rubric §9/§10을 routing trace로 검증.

| ID | repo | 추정 도메인 | rough request | 분기 |
|---|---|---|---|---|
| **T1** | DeepingSource `noisy-arl` | adversarial/robust ML | "robustness AUROC를 trial budget 24회로 자동 탐색 개선" | Analysis/autonomous-optimization |
| **T2** | DeepingSource `asan-med-img-obf-for-distribution` | 의료영상 비식별화 | "de-id 파이프라인 PR(+420/-90 LOC, 6파일) 리뷰 — `**/*dicom*`·`phi/**` 규칙 적용" | **Review/code-review-rubric (§9+§10)** |
| **T3** | `vercel/next.js` (OSS 보충) | web framework | "App Router 도입 vs Pages Router 유지 — 3안 비교 결정" | Decision/multi-agent |

`[ASSUMPTION:DeepingSource 2 repo description 공란 — 도메인 추정 repo 이름 기반. 검증: README webfetch/owner confirm]`
**이질성**: T1=Analysis+autonomous, T2=Review+code-review, T3=Decision+multi-agent — 3 분기 모두 다름.

### A.3.2 Mock trace 요약

#### T1 — `noisy-arl-robustness-auto-tune` (Analysis)
- **Phase 1**: B=Analysis(단일 스칼라 자동 반복), G=REVERSIBLE, L=ml-researcher/security-researcher. ✓ G1.
- **Phase 2**: selected=[task-spec, autonomous-optimization-loop(§3/§4/§6), role-dict(ml-researcher×proposer; security-researcher×devils-advocate)]. bypass=experiment-design(검증 아님)/multi-agent/rfc/code-review/speckit. ✓ G2 (cap 4).
- **Phase 3-5**: §6 Gates=results.tsv+best commit+학습 메모. P1-P5 PASS. ⚠ smell 2.4 — measure 실패 fallback이 component-local(autonomous §3 Step0)이나 composer §5 발췌 boilerplate 부재(B6).

#### T2 — `asan-med-img-deid-pr-review` (Review — ★ v1.1 검증)
- **Phase 1**: B=Review, H=PR(+420/-90, 6파일), G=COSTLY-TO-REVERSE(의료데이터 재배포), L=security-researcher/ml-engineer. ✓ G1.
- **Phase 2**: selected=[task-spec, code-review-rubric(§2/§3/§4 **+§9 custom rule +§10 번들링**), role-dict(architect; security-researcher×evidence-checker)]. bypass=multi-agent(단일 변경)/experiment/rfc/speckit. ✓ G2.
  - **§9 발동**: PR이 `phi/**`·`**/*dicom*` 경로 포함 → repo-local 규칙(예: `phi/** → A2 위협모델 ≥4 강제`, `**/*dicom* → PII redact 차단조건`) 매칭. v1.1 §9.2 우선순위 + §9.4 hard-floor 불가침 적용. routing trace 정상.
  - **§10 발동**: 6파일(≥5) cross-cutting → 번들 휴리스틱 적용(de-id 코드+테스트, 스키마+마이그레이션). v1.1 §10.4 절차 + cross-cutting pass 정상.
- **Phase 3-5**: §6 Gates=8축 점수+§9 적용 rule 출처(§5 신규 절)+§10 bundle별 발견+차단 결정. P1-P5 PASS. ⚠ P5 redaction(환자 ID/병원명 PII placeholder). ⚠ smell 2.4 — code-review-rubric은 §9.6/§10.4-3에 *자체 fallback 보유*(v1.1에서 추가)이나, composer §5 발췌 가이드(B6)가 "fallback 절도 발췌"를 강제하지 않아 *발췌 시 누락 가능* 위험은 잔존.
- **★ v1.1 검증 결론**: code-review-rubric §9/§10이 Review routing trace에서 정상 발동·정합. 직전 세션 빌드 무결성 확인(단, self-build self-audit 편향 → §A.8).

#### T3 — `next-app-router-decision` (Decision)
- **Phase 1**: B=Decision, C=3 topic(App Router/Pages 유지/Hybrid), G=REVERSIBLE, L=frontend-engineer. ✓ G1.
- **Phase 2**: selected=[task-spec, multi-agent-template(§2/§4/§7/§8), role-dict(frontend-engineer×proposer; devils-advocate; judge)]. bypass=rfc/speckit/code-review/experiment/autonomous. ✓ G2.
- **Phase 3-5**: §6 Gates=동일 criteria 비교 매트릭스+synthesis+judge 결정. P1-P5 PASS. ⚠ smell 2.4 — multi-agent §3.1 baseline 읽기 실패 fallback 발췌 누락 가능(B6).

### A.3.3 평가 매트릭스 (P1-P7 + 6 smell) `[self-eval 경고]`

| Test | P1 | P2 | P3 | P4 | P5 | P6 | P7 | 2.1 | 2.2 | 2.3 | 2.4 Missing fallback | 2.5 | 2.6 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **T1** | ✓ | ✓ | ✓ | ✓ | n/a | ⚠ trial dom | ✓ | ✓ | ✓ | ✓ | **⚠ measure 실패 발췌 누락(B6)** | ✓ | ✓ |
| **T2** | ✓ | ✓ | ✓ | ✓ | ⚠ PII placeholder | ✓ ~3k+bundle | ✓ | ✓ | ✓ | ✓ | **⚠ §9/§10 fallback 보유하나 composer 발췌 boilerplate 부재(B6)** | ✓ | ✓ (§10이 over-scope 아님 — 단일 PR 분할) |
| **T3** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | **⚠ baseline 읽기 실패 발췌 누락(B6)** | ✓ | ✓ |

### A.3.4 Synthesis — 반복 발견

> 3 mock 모두 `smell 2.4 Missing fallback` ⚠ — **2026-05-26/05-29와 동일**. component-local fallback은 있으나 composer §5 발췌 가이드에 "fallback 절도 발췌" 횡단 boilerplate 부재(B6). `grep "fallback\|실패 시" composer §5 발췌가이드` → 0. **§5.2 반복 발견 — BLOCKING 유지(B6)**.
> 특이점: T2(code-review v1.1)는 component 자체에 §9.6/§10.4-3 fallback을 **추가 보유**하므로 B6의 영향이 *부분 완화*되나, 근본(composer 발췌 강제력)은 미해결.

---

## A.4 발견 priority 표

### BLOCKING (7건 — B1~B7, 모두 §5.2 반복 발견)

| ID | 이전 | 발견 | 재현 | 수정 권고 | 파일 |
|---|---|---|---|---|---|
| **B1** | M1 | `trigger.md` 표준 머리말 위반 (3회차) | `head -3 trigger.md` 머리말 0 | 표준 3필드 머리말 + "아래 구분선부터 복사 본문" 명시 | `trigger.md` |
| **B2** | M3 | G7 phantom / G6 종착 불일치 | composer:68 "G1-G7" ↔ rubric §7 G6 | G1-G6 통일 + lifecycle 별도 슬롯 명시 | `builder/optimized-prompt-composer.md §1/§6`, `builder/prompt-evaluation-rubric.md §7` |
| **B3** | M4 | Phase 6 실행 실패 분기 부재 | `grep "실행 실패\|회귀.*Mode" composer` 0 | §7 끝 Mode A/B/C 실패 회귀 1단락 | `builder/optimized-prompt-composer.md §7` |
| **B4** | M5 | composed.md 회귀 이력 손실 | composer:246 덮어쓰기 + `.specs/` ignore | `<task-id>.composed.iter-<N>.md` suffix 또는 log diff (옵션 사용자 결정) | `builder/optimized-prompt-composer.md §5` |
| **B5** | M2+M6 | `trigger.md` DRY drift (소비자 2개로 증가) | `grep "A-2\|사본" trigger.md` 0 + CLAUDE.md 등록 0 | trigger.md에 "trigger-prompts.md A-2 paste-ready 사본 — 정본 동기화" 1줄 + CLAUDE.md 인터페이스 등록. B1과 묶음 가능 | `trigger.md`/`CLAUDE.md` |
| **B6** | M7 | composer §5 발췌 가이드에 fallback boilerplate 부재 | 3 mock 모두 smell 2.4 ⚠ | §5 "발췌 가이드"에 "각 component fallback 절도 발췌" 1줄 | `builder/optimized-prompt-composer.md §5` |
| **B7** | L1 | G4 spec.E echo grep 게이트 부재 | composer G4 체크리스트에 spec.E echo 없음 | G4에 "spec.E 각 criterion이 §6에 grep 단위 echo" 1줄 | `builder/optimized-prompt-composer.md §5 G4` |

### HIGH (1건 — 신규)

| ID | 발견 | 재현 | 수정 권고 | 파일 |
|---|---|---|---|---|
| ★ **N3** | `builder/` 리팩터(`f86cc2a`)가 audit 자체 기계 검사에 미반영 — (a) G0 command가 `builder/*.md` 미스캔 → 14 카운트로 ≥18 거짓 FAIL (실제 19), (b) §3.1 header 루프가 builder 5파일 미검사 | `ls *.md components/*.md | wc -l` = 14 vs `+builder/*.md` = 19; §3.1 루프에 builder 경로 없음 | audit §6 G0 command + §3.1 루프에 `prompts/prompt-composer-system/builder/*.md` 추가. §5.1 reference는 이미 builder 경로 보유 → 내부 일관성 회복 | `audit-composer-system.md §3.1, §6 G0` |

### MEDIUM (1건 — 신규)

| ID | 발견 | 재현 | 수정 권고 | 파일 |
|---|---|---|---|---|
| ★ **N4** | §3.5 S5 grep ERE `\|` 오용 → false FAIL(0), 수정 시 13 | S5 원본 0 hit / 수정 패턴 13 hit | S5 command의 `\|`(literal)를 alternation `|`로 교정: `grep -cE "^- \*\*Step [0-5]|^\| \*\*G[0-5]\*\*|^\| Gate "` | `audit-composer-system.md §3.5 S5` |

### LOW (4건)

| ID | 발견 | 비고 |
|---|---|---|
| **L2** | role-dict §5 vision/CV lens 부재 **확정(VERIFIED 격상)** | §5 전체 read 완료 — CV lens 없음. DeepingSource 의료영상 도메인 고려 시 component-discovery collect 후보. enhancement(breakage 아님) |
| **N1** | CLAUDE.md 자기 규약 위반(머리말) — 반복 2회차 | 규약 SSOT이므로 1줄 예외 조항 권고. recurring이나 LOW 유지 |
| **L3** | composer §0 frontmatter default 모드 식별자 부재 | UNRESOLVED. 정보성 LOW |
| **L4** | README.md에 prompt-composer-system 절 없음 | UNRESOLVED. onboarding 영향 |
| **N5** | (positive) code-review-rubric v1.1 무결 — 회귀 0 | 정보. self-build self-audit 편향 주의(§A.8) |

### Δ from 2026-05-29 요약

| 분류 | 2026-05-29 | 2026-06-07 | 변화 |
|---|---|---|---|
| BLOCKING | 8 | **7** | -1 (**B0 RESOLVED** `d761a0f`); B1-B7 전원 유지(recurring) |
| HIGH | 0 | **1** | +1 (**N3** builder/ drift — 신규) |
| MEDIUM | 1 (M3-redux) | **1** | M3-redux 유지/dismiss + **N4** 신규 |
| LOW | 5 | 4-5 | L2 evidence 격상(VERIFIED), N1/L3/L4 유지, N5 positive 추가 |
| RESOLVED | H2 | **B0** | B0(H1 계열) 최종 해소 |

---

## A.5 self_upgrade "1회 1대상" 단위 PR 제안

> `self_upgrade.md §0` 1회 1대상 + 작은 diff + 사람 승인 게이트. 본 audit run mutation 0 — 모든 PR은 사용자 승인 후 별도 conversation.

**신규/우선 PR (이번 run):**
- **PR-N3 [HIGH]** `fix(audit): include builder/ in G0 count + §3.1 header loop` — audit §6 G0 command와 §3.1 루프에 `builder/*.md` 추가. 검증: `ls ... builder/*.md ... | wc -l` ≥ 18 + 5 builder 파일 머리말 검사 포함. **audit 자체 변경 → self_upgrade 거버넌스(1회1대상) + 블라인드 채점 권고.**
- **PR-N4 [MEDIUM]** `fix(audit): correct S5 self-check grep alternation` — §3.5 S5 패턴 `\|`→`|` 교정. 검증: S5 ≥10 hit.

**반복 BLOCKING PR (2026-05-29에서 carry-over, 미해결):**
- **PR-B1+B5** `fix(trigger.md): standard header + A-2 DRY note + CLAUDE.md interface 등록`.
- **PR-B2** `docs(composer): unify G1-G6 + remove G7 phantom` (composer §1/§6 + rubric §7).
- **PR-B3** `docs(composer): add Phase 6 execution failure routing` (§7).
- **PR-B4** `docs(composer): preserve composed.md regression history` (§5, 옵션 사용자 결정).
- **PR-B6** `docs(composer): require fallback excerpt in component guide` (§5).
- **PR-B7** `docs(composer): G4 require spec.E echo grep` (§5 G4).

**LOW 배치:**
- PR-N1(CLAUDE.md 예외 조항), PR-L2(role-dict §5 vision lens — **component-discovery collect로 회부 권고**: ledger 후보 적재 경로가 audit 직접 수정보다 거버넌스 적합), PR-L3/L4.

> **권고 우선순위**: ① N3(audit 자기 무결성 — 다른 audit 신뢰성의 전제) → ② N4 → ③ B2/B6/B7(composer 1줄 diff, 저비용 고효과) → ④ B1+B5(trigger.md 묶음) → ⑤ B3/B4(설계 결정 필요).
> **자동화 권고**: B1-B7이 3회 누적. `grep` 가드를 pre-commit/CI에 추가(B0 fix 패턴처럼) + audit `/schedule` 정례화 검토.

---

## A.6 출처 (tier별)

### Tier 1 (repo 내부 SSOT) `[VERIFIED:Read/bash @2026-06-07]`
- `CLAUDE.md`, `audit-composer-system.md`, `builder/{optimized-prompt-composer,prompt-component-router,prompt-evaluation-rubric,task-spec-template,context-injection-patterns}.md`, `trigger.md`, `trigger-prompts.md`, `component-discovery-{collect,approve,ledger}.md`, `components/*.md` 7파일(code-review-rubric 전문 + agent-role-dictionary 전문 §5 포함), `self_upgrade.md`, `self_upgrade-changelog.md`, `.gitignore`.
- git log: `f86cc2a`(builder refactor), `d761a0f`(B0 fix), `7f800be`(code-review v1.1).

### Tier 1.5 (이전 audit — 반복 발견 격상 근거)
- `docs/projects/composer-system-audit/2026-05-26.md`, `2026-05-29.md` `[VERIFIED:Read 2026-05-29 @2026-06-07]`.

### Tier 2 (외부 — gh CLI)
- `[VERIFIED:bash gh repo list DeepingSource --visibility public @2026-06-07]` 2건.

### Tier 3 (추정)
- `[ASSUMPTION:DeepingSource 2 repo 도메인 = repo 이름 기반 — README webfetch/owner confirm 필요]`.
- `[ASSUMPTION:vercel/next.js는 routing 분기 보충용 — 실제 도입 검토 아님]`.

---

## A.7 위험 / 보류 사항

- **[self-eval 경고]** 본 보고서의 모든 점수·매트릭스·priority는 메인 세션 자기 진단. self_upgrade §B4 — 별도 LLM(`codex-bridge`/`gemini-bridge`) 또는 별도 Claude 세션에 본 보고서+audit prompt를 블라인드 입력으로 전달해 재채점 권고.
- ★ **self-build self-audit 편향(N5)**: 본 audit auditor가 *같은 세션에서* code-review-rubric v1.1을 빌드한 뒤 audit함. T2의 "v1.1 정상" 판정은 anchoring 위험 → **반드시 별도 세션/LLM 블라인드 재검토**. (Compose/Execute/Evaluate 세션 분리 Invariant의 정신과 동일.)
- **N3는 audit 자기 무결성 문제** — audit이 자신의 검사 sample에서 5/19 파일(builder)을 놓치는 상태. 다른 모든 audit 결과의 신뢰성 전제이므로 **PR-N3 최우선** 권고.
- **7 BLOCKING 3회 누적** — 사용자가 의도적 deferred라면 audit §5.2에 dismiss 처리 조항(격상 후 사용자 dismiss 시 차기 LOW 재기록) 보강 검토 권고.
- **본 audit run mutation 0** — 본 보고서 Write만 read-only 산출(§7 예외). PR은 Step 5 사용자 승인 후 별도 conversation.

## A.8 메타 — 본 audit 한계

1. `multi-agent-analysis-template.md` 부분 read(§0-§1) — §2-§8 미확인(직전 run과 동일).
2. ★ **self-build self-audit**: code-review-rubric v1.1을 본 세션 auditor가 빌드 → self-eval 편향(N5/§A.7). 블라인드 재검토 필수.
3. 본 audit이 *자기 자신*도 점검 대상(§3.5) — S5 command 버그(N4)·G0/§3.1 builder 누락(N3)을 자가 발견했으나, 자가 진단이므로 blind external review 권고.
4. L2 vision/CV lens는 본 run에서 §5 read로 VERIFIED 격상했으나 *lens 추가 설계*는 component-discovery collect 경로로 회부 권고(audit 직접 수정 아님).

---

*보고서 끝. 본 audit run mutation 0(본 보고서 Write만). PR 제안은 Step 5 사용자 승인 후 별도 conversation에서 1회1대상으로 진행. 다음 audit: B1-B7 + N3/N4 해소 여부 + builder/ 구조 안정성 재확인.*
